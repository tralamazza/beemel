// Generated by bml-svd
// Vendor: Raspberry Pi
// Device: RP2350 (0.1)
// 
//         Dual Cortex-M33 or Hazard3 processors at 150MHz
//         520kB on-chip SRAM, in 10 independent banks
//         Extended low-power sleep states with optional SRAM retention: as low as 10uA DVDD
//         8kB of one-time-programmable storage (OTP)
//         Up to 16MB of external QSPI flash/PSRAM via dedicated QSPI bus
//             Additional 16MB flash/PSRAM accessible via optional second chip-select
//         On-chip switched-mode power supply to generate core voltage
//             Low-quiescent-current LDO mode can be enabled for sleep states
//         2x on-chip PLLs for internal or external clock generation
//         GPIOs are 5V-tolerant (powered), and 3.3V-failsafe (unpowered)
//         Security features:
//             Optional boot signing, enforced by on-chip mask ROM, with key fingerprint in OTP
//             Protected OTP storage for optional boot decryption key
//             Global bus filtering based on Arm or RISC-V security/privilege levels
//             Peripherals, GPIOs and DMA channels individually assignable to security domains
//             Hardware mitigations for fault injection attacks
//             Hardware SHA-256 accelerator
//         Peripherals:
//             2x UARTs
//             2x SPI controllers
//             2x I2C controllers
//             24x PWM channels
//             USB 1.1 controller and PHY, with host and device support
//             12x PIO state machines
//             1x HSTX peripheral
//     

// TEAL registers accessible through the debug interface
peripheral PPB_NS at 0xE0020000 {
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM0 offset 0x00 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM1 offset 0x04 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM2 offset 0x08 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM3 offset 0x0C {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM4 offset 0x10 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM5 offset 0x14 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM6 offset 0x18 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM7 offset 0x1C {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM8 offset 0x20 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM9 offset 0x24 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM10 offset 0x28 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM11 offset 0x2C {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM12 offset 0x30 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM13 offset 0x34 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM14 offset 0x38 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM15 offset 0x3C {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM16 offset 0x40 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM17 offset 0x44 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM18 offset 0x48 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM19 offset 0x4C {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM20 offset 0x50 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM21 offset 0x54 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM22 offset 0x58 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM23 offset 0x5C {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM24 offset 0x60 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM25 offset 0x64 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM26 offset 0x68 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM27 offset 0x6C {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM28 offset 0x70 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM29 offset 0x74 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM30 offset 0x78 {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provides the interface for generating Instrumentation packets
    reg ITM_STIM31 offset 0x7C {
        field STIMULUS: u32 bit[0..31]  // Data to write to the Stimulus Port FIFO, for forwarding as an Instrumentation packet. The size of write access determines the type of Instrumentation packet generated.
    }
    // Provide an individual enable bit for each ITM_STIM register
    reg ITM_TER0 offset 0xE00 {
        field STIMENA: u32 bit[0..31]  // For STIMENA[m] in ITM_TER*n, controls whether ITM_STIM(32*n + m) is enabled
    }
    // Controls which stimulus ports can be accessed by unprivileged code
    reg ITM_TPR offset 0xE40 {
        field PRIVMASK: u32 bit[0..3]  // Bit mask to enable tracing on ITM stimulus ports
    }
    // Configures and controls transfers through the ITM interface
    reg ITM_TCR offset 0xE80 {
        field BUSY: b1 bit[23] readonly  // Indicates whether the ITM is currently processing events
        field TRACEBUSID: u32 bit[16..22]  // Identifier for multi-source trace stream formatting. If multi-source trace is in use, the debugger must write a unique non-zero trace ID value to this field
        field GTSFREQ: u32 bit[10..11]  // Defines how often the ITM generates a global timestamp, based on the global timestamp clock frequency, or disables generation of global timestamps
        field TSPRESCALE: u32 bit[8..9]  // Local timestamp prescaler, used with the trace packet reference clock
        field STALLENA: b1 bit[5]  // Stall the PE to guarantee delivery of Data Trace packets.
        field SWOENA: b1 bit[4]  // Enables asynchronous clocking of the timestamp counter
        field TXENA: b1 bit[3]  // Enables forwarding of hardware event packet from the DWT unit to the ITM for output to the TPIU
        field SYNCENA: b1 bit[2]  // Enables Synchronization packet transmission for a synchronous TPIU
        field TSENA: b1 bit[1]  // Enables Local timestamp generation
        field ITMENA: b1 bit[0]  // Enables the ITM
    }
    // Integration Mode: Read ATB Ready
    reg INT_ATREADY offset 0xEF0 {
        field AFVALID: b1 bit[1] readonly  // A read of this bit returns the value of AFVALID
        field ATREADY: b1 bit[0] readonly  // A read of this bit returns the value of ATREADY
    }
    // Integration Mode: Write ATB Valid
    reg INT_ATVALID offset 0xEF8 {
        field AFREADY: b1 bit[1]  // A write to this bit gives the value of AFREADY
        field ATREADY: b1 bit[0]  // A write to this bit gives the value of ATVALID
    }
    // Integration Mode Control Register
    reg ITM_ITCTRL offset 0xF00 {
        field IME: b1 bit[0]  // Integration mode enable bit - The possible values are:  0 - The trace unit is not in integration mode. 1 - The trace unit is in integration mode. This mode enables: A debug agent to perform topology detection. SoC test software to perform integration testing.
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_DEVARCH offset 0xFBC {
        field ARCHITECT: u32 bit[21..31] readonly  // Defines the architect of the component. Bits [31:28] are the JEP106 continuation code (JEP106 bank ID, minus 1) and bits [27:21] are the JEP106 ID code.
        field PRESENT: b1 bit[20] readonly  // Defines that the DEVARCH register is present
        field REVISION: u32 bit[16..19] readonly  // Defines the architecture revision of the component
        field ARCHVER: u32 bit[12..15] readonly  // Defines the architecture version of the component
        field ARCHPART: u32 bit[0..11] readonly  // Defines the architecture of the component
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_DEVTYPE offset 0xFCC {
        field SUB: u32 bit[4..7] readonly  // Component sub-type
        field MAJOR: u32 bit[0..3] readonly  // Component major type
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_PIDR4 offset 0xFD0 {
        field SIZE: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field DES_2: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_PIDR5 offset 0xFD4 {
        field ITM_PIDR5: u32 bit[0..31]
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_PIDR6 offset 0xFD8 {
        field ITM_PIDR6: u32 bit[0..31]
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_PIDR7 offset 0xFDC {
        field ITM_PIDR7: u32 bit[0..31]
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_PIDR0 offset 0xFE0 {
        field PART_0: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_PIDR1 offset 0xFE4 {
        field DES_0: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field PART_1: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_PIDR2 offset 0xFE8 {
        field REVISION: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field JEDEC: b1 bit[3] readonly  // See CoreSight Architecture Specification
        field DES_1: u32 bit[0..2] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_PIDR3 offset 0xFEC {
        field REVAND: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field CMOD: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_CIDR0 offset 0xFF0 {
        field PRMBL_0: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_CIDR1 offset 0xFF4 {
        field CLASS: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field PRMBL_1: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_CIDR2 offset 0xFF8 {
        field PRMBL_2: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the ITM
    reg ITM_CIDR3 offset 0xFFC {
        field PRMBL_3: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides configuration and status information for the DWT unit, and used to control features of the unit
    reg DWT_CTRL offset 0x1000 {
        field NUMCOMP: u32 bit[28..31] readonly  // Number of DWT comparators implemented
        field NOTRCPKT: b1 bit[27] readonly  // Indicates whether the implementation does not support trace
        field NOEXTTRIG: b1 bit[26] readonly  // Reserved, RAZ
        field NOCYCCNT: b1 bit[25] readonly  // Indicates whether the implementation does not include a cycle counter
        field NOPRFCNT: b1 bit[24] readonly  // Indicates whether the implementation does not include the profiling counters
        field CYCDISS: b1 bit[23]  // Controls whether the cycle counter is disabled in Secure state
        field CYCEVTENA: b1 bit[22]  // Enables Event Counter packet generation on POSTCNT underflow
        field FOLDEVTENA: b1 bit[21]  // Enables DWT_FOLDCNT counter
        field LSUEVTENA: b1 bit[20]  // Enables DWT_LSUCNT counter
        field SLEEPEVTENA: b1 bit[19]  // Enable DWT_SLEEPCNT counter
        field EXCEVTENA: b1 bit[18]  // Enables DWT_EXCCNT counter
        field CPIEVTENA: b1 bit[17]  // Enables DWT_CPICNT counter
        field EXTTRCENA: b1 bit[16]  // Enables generation of Exception Trace packets
        field PCSAMPLENA: b1 bit[12]  // Enables use of POSTCNT counter as a timer for Periodic PC Sample packet generation
        field SYNCTAP: u32 bit[10..11]  // Selects the position of the synchronization packet counter tap on the CYCCNT counter. This determines the Synchronization packet rate
        field CYCTAP: b1 bit[9]  // Selects the position of the POSTCNT tap on the CYCCNT counter
        field POSTINIT: u32 bit[5..8]  // Initial value for the POSTCNT counter
        field POSTPRESET: u32 bit[1..4]  // Reload value for the POSTCNT counter
        field CYCCNTENA: b1 bit[0]  // Enables CYCCNT
    }
    // Shows or sets the value of the processor cycle counter, CYCCNT
    reg DWT_CYCCNT offset 0x1004 {
        field CYCCNT: u32 bit[0..31]  // Increments one on each processor clock cycle when DWT_CTRL.CYCCNTENA == 1 and DEMCR.TRCENA == 1. On overflow, CYCCNT wraps to zero
    }
    // Counts the total cycles spent in exception processing
    reg DWT_EXCCNT offset 0x100C {
        field EXCCNT: u32 bit[0..7]  // Counts one on each cycle when all of the following are true: - DWT_CTRL.EXCEVTENA == 1 and DEMCR.TRCENA == 1. - No instruction is executed, see DWT_CPICNT. - An exception-entry or exception-exit related operation is in progress. - Either SecureNoninvasiveDebugAllowed() == TRUE, or NS-Req for the operation is set to Non-secure and NoninvasiveDebugAllowed() == TRUE.
    }
    // Increments on the additional cycles required to execute all load or store instructions
    reg DWT_LSUCNT offset 0x1014 {
        field LSUCNT: u32 bit[0..7]  // Counts one on each cycle when all of the following are true: - DWT_CTRL.LSUEVTENA == 1 and DEMCR.TRCENA == 1. - No instruction is executed, see DWT_CPICNT. - No exception-entry or exception-exit operation is in progress, see DWT_EXCCNT. - A load-store operation is in progress. - Either SecureNoninvasiveDebugAllowed() == TRUE, or NS-Req for the operation is set to Non-secure and NoninvasiveDebugAllowed() == TRUE.
    }
    // Increments on the additional cycles required to execute all load or store instructions
    reg DWT_FOLDCNT offset 0x1018 {
        field FOLDCNT: u32 bit[0..7]  // Counts on each cycle when all of the following are true: - DWT_CTRL.FOLDEVTENA == 1 and DEMCR.TRCENA == 1. - At least two instructions are executed, see DWT_CPICNT. - Either SecureNoninvasiveDebugAllowed() == TRUE, or the PE is in Non-secure state and NoninvasiveDebugAllowed() == TRUE. The counter is incremented by the number of instructions executed, minus one
    }
    // Provides a reference value for use by watchpoint comparator 0
    reg DWT_COMP0 offset 0x1020 {
        field DWT_COMP0: u32 bit[0..31]
    }
    // Controls the operation of watchpoint comparator 0
    reg DWT_FUNCTION0 offset 0x1028 {
        field ID: u32 bit[27..31] readonly  // Identifies the capabilities for MATCH for comparator *n
        field MATCHED: b1 bit[24] readonly  // Set to 1 when the comparator matches
        field DATAVSIZE: u32 bit[10..11]  // Defines the size of the object being watched for by Data Value and Data Address comparators
        field ACTION: u32 bit[4..5]  // Defines the action on a match. This field is ignored and the comparator generates no actions if it is disabled by MATCH
        field MATCH: u32 bit[0..3]  // Controls the type of match generated by this comparator
    }
    // Provides a reference value for use by watchpoint comparator 1
    reg DWT_COMP1 offset 0x1030 {
        field DWT_COMP1: u32 bit[0..31]
    }
    // Controls the operation of watchpoint comparator 1
    reg DWT_FUNCTION1 offset 0x1038 {
        field ID: u32 bit[27..31] readonly  // Identifies the capabilities for MATCH for comparator *n
        field MATCHED: b1 bit[24] readonly  // Set to 1 when the comparator matches
        field DATAVSIZE: u32 bit[10..11]  // Defines the size of the object being watched for by Data Value and Data Address comparators
        field ACTION: u32 bit[4..5]  // Defines the action on a match. This field is ignored and the comparator generates no actions if it is disabled by MATCH
        field MATCH: u32 bit[0..3]  // Controls the type of match generated by this comparator
    }
    // Provides a reference value for use by watchpoint comparator 2
    reg DWT_COMP2 offset 0x1040 {
        field DWT_COMP2: u32 bit[0..31]
    }
    // Controls the operation of watchpoint comparator 2
    reg DWT_FUNCTION2 offset 0x1048 {
        field ID: u32 bit[27..31] readonly  // Identifies the capabilities for MATCH for comparator *n
        field MATCHED: b1 bit[24] readonly  // Set to 1 when the comparator matches
        field DATAVSIZE: u32 bit[10..11]  // Defines the size of the object being watched for by Data Value and Data Address comparators
        field ACTION: u32 bit[4..5]  // Defines the action on a match. This field is ignored and the comparator generates no actions if it is disabled by MATCH
        field MATCH: u32 bit[0..3]  // Controls the type of match generated by this comparator
    }
    // Provides a reference value for use by watchpoint comparator 3
    reg DWT_COMP3 offset 0x1050 {
        field DWT_COMP3: u32 bit[0..31]
    }
    // Controls the operation of watchpoint comparator 3
    reg DWT_FUNCTION3 offset 0x1058 {
        field ID: u32 bit[27..31] readonly  // Identifies the capabilities for MATCH for comparator *n
        field MATCHED: b1 bit[24] readonly  // Set to 1 when the comparator matches
        field DATAVSIZE: u32 bit[10..11]  // Defines the size of the object being watched for by Data Value and Data Address comparators
        field ACTION: u32 bit[4..5]  // Defines the action on a match. This field is ignored and the comparator generates no actions if it is disabled by MATCH
        field MATCH: u32 bit[0..3]  // Controls the type of match generated by this comparator
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_DEVARCH offset 0x1FBC {
        field ARCHITECT: u32 bit[21..31] readonly  // Defines the architect of the component. Bits [31:28] are the JEP106 continuation code (JEP106 bank ID, minus 1) and bits [27:21] are the JEP106 ID code.
        field PRESENT: b1 bit[20] readonly  // Defines that the DEVARCH register is present
        field REVISION: u32 bit[16..19] readonly  // Defines the architecture revision of the component
        field ARCHVER: u32 bit[12..15] readonly  // Defines the architecture version of the component
        field ARCHPART: u32 bit[0..11] readonly  // Defines the architecture of the component
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_DEVTYPE offset 0x1FCC {
        field SUB: u32 bit[4..7] readonly  // Component sub-type
        field MAJOR: u32 bit[0..3] readonly  // Component major type
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_PIDR4 offset 0x1FD0 {
        field SIZE: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field DES_2: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_PIDR5 offset 0x1FD4 {
        field DWT_PIDR5: u32 bit[0..31]
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_PIDR6 offset 0x1FD8 {
        field DWT_PIDR6: u32 bit[0..31]
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_PIDR7 offset 0x1FDC {
        field DWT_PIDR7: u32 bit[0..31]
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_PIDR0 offset 0x1FE0 {
        field PART_0: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_PIDR1 offset 0x1FE4 {
        field DES_0: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field PART_1: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_PIDR2 offset 0x1FE8 {
        field REVISION: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field JEDEC: b1 bit[3] readonly  // See CoreSight Architecture Specification
        field DES_1: u32 bit[0..2] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_PIDR3 offset 0x1FEC {
        field REVAND: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field CMOD: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_CIDR0 offset 0x1FF0 {
        field PRMBL_0: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_CIDR1 offset 0x1FF4 {
        field CLASS: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field PRMBL_1: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_CIDR2 offset 0x1FF8 {
        field PRMBL_2: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the DWT
    reg DWT_CIDR3 offset 0x1FFC {
        field PRMBL_3: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides FPB implementation information, and the global enable for the FPB unit
    reg FP_CTRL offset 0x2000 {
        field REV: u32 bit[28..31] readonly  // Flash Patch and Breakpoint Unit architecture revision
        field NUM_CODE_14_12_: u32 bit[12..14] readonly  // Indicates the number of implemented instruction address comparators. Zero indicates no Instruction Address comparators are implemented. The Instruction Address comparators are numbered from 0 to NUM_CODE - 1
        field NUM_LIT: u32 bit[8..11] readonly  // Indicates the number of implemented literal address comparators. The Literal Address comparators are numbered from NUM_CODE to NUM_CODE + NUM_LIT - 1
        field NUM_CODE_7_4_: u32 bit[4..7] readonly  // Indicates the number of implemented instruction address comparators. Zero indicates no Instruction Address comparators are implemented. The Instruction Address comparators are numbered from 0 to NUM_CODE - 1
        field KEY: b1 bit[1]  // Writes to the FP_CTRL are ignored unless KEY is concurrently written to one
        field ENABLE: b1 bit[0]  // Enables the FPB
    }
    // Indicates whether the implementation supports Flash Patch remap and, if it does, holds the target address for remap
    reg FP_REMAP offset 0x2004 {
        field RMPSPT: b1 bit[29] readonly  // Indicates whether the FPB unit supports the Flash Patch remap function
        field REMAP: u32 bit[5..28] readonly  // Holds the bits[28:5] of the Flash Patch remap address
    }
    // Holds an address for comparison. The effect of the match depends on the configuration of the FPB and whether the comparator is an instruction address comparator or a literal address comparator
    reg FP_COMP0 offset 0x2008 {
        field BE: b1 bit[0]  // Selects between flashpatch and breakpoint functionality
    }
    // Holds an address for comparison. The effect of the match depends on the configuration of the FPB and whether the comparator is an instruction address comparator or a literal address comparator
    reg FP_COMP1 offset 0x200C {
        field BE: b1 bit[0]  // Selects between flashpatch and breakpoint functionality
    }
    // Holds an address for comparison. The effect of the match depends on the configuration of the FPB and whether the comparator is an instruction address comparator or a literal address comparator
    reg FP_COMP2 offset 0x2010 {
        field BE: b1 bit[0]  // Selects between flashpatch and breakpoint functionality
    }
    // Holds an address for comparison. The effect of the match depends on the configuration of the FPB and whether the comparator is an instruction address comparator or a literal address comparator
    reg FP_COMP3 offset 0x2014 {
        field BE: b1 bit[0]  // Selects between flashpatch and breakpoint functionality
    }
    // Holds an address for comparison. The effect of the match depends on the configuration of the FPB and whether the comparator is an instruction address comparator or a literal address comparator
    reg FP_COMP4 offset 0x2018 {
        field BE: b1 bit[0]  // Selects between flashpatch and breakpoint functionality
    }
    // Holds an address for comparison. The effect of the match depends on the configuration of the FPB and whether the comparator is an instruction address comparator or a literal address comparator
    reg FP_COMP5 offset 0x201C {
        field BE: b1 bit[0]  // Selects between flashpatch and breakpoint functionality
    }
    // Holds an address for comparison. The effect of the match depends on the configuration of the FPB and whether the comparator is an instruction address comparator or a literal address comparator
    reg FP_COMP6 offset 0x2020 {
        field BE: b1 bit[0]  // Selects between flashpatch and breakpoint functionality
    }
    // Holds an address for comparison. The effect of the match depends on the configuration of the FPB and whether the comparator is an instruction address comparator or a literal address comparator
    reg FP_COMP7 offset 0x2024 {
        field BE: b1 bit[0]  // Selects between flashpatch and breakpoint functionality
    }
    // Provides CoreSight discovery information for the FPB
    reg FP_DEVARCH offset 0x2FBC {
        field ARCHITECT: u32 bit[21..31] readonly  // Defines the architect of the component. Bits [31:28] are the JEP106 continuation code (JEP106 bank ID, minus 1) and bits [27:21] are the JEP106 ID code.
        field PRESENT: b1 bit[20] readonly  // Defines that the DEVARCH register is present
        field REVISION: u32 bit[16..19] readonly  // Defines the architecture revision of the component
        field ARCHVER: u32 bit[12..15] readonly  // Defines the architecture version of the component
        field ARCHPART: u32 bit[0..11] readonly  // Defines the architecture of the component
    }
    // Provides CoreSight discovery information for the FPB
    reg FP_DEVTYPE offset 0x2FCC {
        field SUB: u32 bit[4..7] readonly  // Component sub-type
        field MAJOR: u32 bit[0..3] readonly  // Component major type
    }
    // Provides CoreSight discovery information for the FP
    reg FP_PIDR4 offset 0x2FD0 {
        field SIZE: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field DES_2: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the FP
    reg FP_PIDR5 offset 0x2FD4 {
        field FP_PIDR5: u32 bit[0..31]
    }
    // Provides CoreSight discovery information for the FP
    reg FP_PIDR6 offset 0x2FD8 {
        field FP_PIDR6: u32 bit[0..31]
    }
    // Provides CoreSight discovery information for the FP
    reg FP_PIDR7 offset 0x2FDC {
        field FP_PIDR7: u32 bit[0..31]
    }
    // Provides CoreSight discovery information for the FP
    reg FP_PIDR0 offset 0x2FE0 {
        field PART_0: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the FP
    reg FP_PIDR1 offset 0x2FE4 {
        field DES_0: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field PART_1: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the FP
    reg FP_PIDR2 offset 0x2FE8 {
        field REVISION: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field JEDEC: b1 bit[3] readonly  // See CoreSight Architecture Specification
        field DES_1: u32 bit[0..2] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the FP
    reg FP_PIDR3 offset 0x2FEC {
        field REVAND: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field CMOD: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the FP
    reg FP_CIDR0 offset 0x2FF0 {
        field PRMBL_0: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the FP
    reg FP_CIDR1 offset 0x2FF4 {
        field CLASS: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field PRMBL_1: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the FP
    reg FP_CIDR2 offset 0x2FF8 {
        field PRMBL_2: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the FP
    reg FP_CIDR3 offset 0x2FFC {
        field PRMBL_3: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides information about the interrupt controller
    reg ICTR offset 0xE004 {
        field INTLINESNUM: u32 bit[0..3] readonly  // Indicates the number of the highest implemented register in each of the NVIC control register sets, or in the case of NVIC_IPR*n, 4×INTLINESNUM
    }
    // Provides IMPLEMENTATION DEFINED configuration and control options
    reg ACTLR offset 0xE008 {
        field EXTEXCLALL: b1 bit[29]  // External Exclusives Allowed with no MPU
        field DISITMATBFLUSH: b1 bit[12]  // Disable ATB Flush
        field FPEXCODIS: b1 bit[10]  // Disable FPU exception outputs
        field DISOOFP: b1 bit[9]  // Disable out-of-order FP instruction completion
        field DISFOLD: b1 bit[2]  // Disable dual-issue.
        field DISMCYCINT: b1 bit[0]  // Disable dual-issue.
    }
    // Use the SysTick Control and Status Register to enable the SysTick features.
    reg SYST_CSR offset 0xE010 {
        field COUNTFLAG: b1 bit[16] readonly  // Returns 1 if timer counted to 0 since last time this was read. Clears on read by application or debugger.
        field CLKSOURCE: b1 bit[2]  // SysTick clock source. Always reads as one if SYST_CALIB reports NOREF. 
        field TICKINT: b1 bit[1]  // Enables SysTick exception request: 
        field ENABLE: b1 bit[0]  // Enable SysTick counter: 
    }
    // Use the SysTick Reload Value Register to specify the start value to load into the current value register when the counter reaches 0. It can be any value between 0 and 0x00FFFFFF. A start value of 0 is possible, but has no effect because the SysTick interrupt and COUNTFLAG are activated when counting from 1 to 0. The reset value of this register is UNKNOWN. 
    reg SYST_RVR offset 0xE014 {
        field RELOAD: u32 bit[0..23]  // Value to load into the SysTick Current Value Register when the counter reaches 0.
    }
    // Use the SysTick Current Value Register to find the current value in the register. The reset value of this register is UNKNOWN.
    reg SYST_CVR offset 0xE018 {
        field CURRENT: u32 bit[0..23]  // Reads return the current value of the SysTick counter. This register is write-clear. Writing to it with any value clears the register to 0. Clearing this register also clears the COUNTFLAG bit of the SysTick Control and Status Register.
    }
    // Use the SysTick Calibration Value Register to enable software to scale to any required speed using divide and multiply.
    reg SYST_CALIB offset 0xE01C {
        field NOREF: b1 bit[31] readonly  // If reads as 1, the Reference clock is not provided - the CLKSOURCE bit of the SysTick Control and Status register will be forced to 1 and cannot be cleared to 0.
        field SKEW: b1 bit[30] readonly  // If reads as 1, the calibration value for 10ms is inexact (due to clock frequency).
        field TENMS: u32 bit[0..23] readonly  // An optional Reload value to be used for 10ms (100Hz) timing, subject to system clock skew errors. If the value reads as 0, the calibration value is not known.
    }
    // Enables or reads the enabled state of each group of 32 interrupts
    reg NVIC_ISER0 offset 0xE100 {
        field SETENA: u32 bit[0..31]  // For SETENA[m] in NVIC_ISER*n, indicates whether interrupt 32*n + m is enabled
    }
    // Enables or reads the enabled state of each group of 32 interrupts
    reg NVIC_ISER1 offset 0xE104 {
        field SETENA: u32 bit[0..31]  // For SETENA[m] in NVIC_ISER*n, indicates whether interrupt 32*n + m is enabled
    }
    // Clears or reads the enabled state of each group of 32 interrupts
    reg NVIC_ICER0 offset 0xE180 {
        field CLRENA: u32 bit[0..31]  // For CLRENA[m] in NVIC_ICER*n, indicates whether interrupt 32*n + m is enabled
    }
    // Clears or reads the enabled state of each group of 32 interrupts
    reg NVIC_ICER1 offset 0xE184 {
        field CLRENA: u32 bit[0..31]  // For CLRENA[m] in NVIC_ICER*n, indicates whether interrupt 32*n + m is enabled
    }
    // Enables or reads the pending state of each group of 32 interrupts
    reg NVIC_ISPR0 offset 0xE200 {
        field SETPEND: u32 bit[0..31]  // For SETPEND[m] in NVIC_ISPR*n, indicates whether interrupt 32*n + m is pending
    }
    // Enables or reads the pending state of each group of 32 interrupts
    reg NVIC_ISPR1 offset 0xE204 {
        field SETPEND: u32 bit[0..31]  // For SETPEND[m] in NVIC_ISPR*n, indicates whether interrupt 32*n + m is pending
    }
    // Clears or reads the pending state of each group of 32 interrupts
    reg NVIC_ICPR0 offset 0xE280 {
        field CLRPEND: u32 bit[0..31]  // For CLRPEND[m] in NVIC_ICPR*n, indicates whether interrupt 32*n + m is pending
    }
    // Clears or reads the pending state of each group of 32 interrupts
    reg NVIC_ICPR1 offset 0xE284 {
        field CLRPEND: u32 bit[0..31]  // For CLRPEND[m] in NVIC_ICPR*n, indicates whether interrupt 32*n + m is pending
    }
    // For each group of 32 interrupts, shows the active state of each interrupt
    reg NVIC_IABR0 offset 0xE300 {
        field ACTIVE: u32 bit[0..31]  // For ACTIVE[m] in NVIC_IABR*n, indicates the active state for interrupt 32*n+m
    }
    // For each group of 32 interrupts, shows the active state of each interrupt
    reg NVIC_IABR1 offset 0xE304 {
        field ACTIVE: u32 bit[0..31]  // For ACTIVE[m] in NVIC_IABR*n, indicates the active state for interrupt 32*n+m
    }
    // For each group of 32 interrupts, determines whether each interrupt targets Non-secure or Secure state
    reg NVIC_ITNS0 offset 0xE380 {
        field ITNS: u32 bit[0..31]  // For ITNS[m] in NVIC_ITNS*n, `IAAMO the target Security state for interrupt 32*n+m
    }
    // For each group of 32 interrupts, determines whether each interrupt targets Non-secure or Secure state
    reg NVIC_ITNS1 offset 0xE384 {
        field ITNS: u32 bit[0..31]  // For ITNS[m] in NVIC_ITNS*n, `IAAMO the target Security state for interrupt 32*n+m
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR0 offset 0xE400 {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR1 offset 0xE404 {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR2 offset 0xE408 {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR3 offset 0xE40C {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR4 offset 0xE410 {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR5 offset 0xE414 {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR6 offset 0xE418 {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR7 offset 0xE41C {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR8 offset 0xE420 {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR9 offset 0xE424 {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR10 offset 0xE428 {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR11 offset 0xE42C {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR12 offset 0xE430 {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR13 offset 0xE434 {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR14 offset 0xE438 {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Sets or reads interrupt priorities
    reg NVIC_IPR15 offset 0xE43C {
        field PRI_N3: u32 bit[28..31]  // For register NVIC_IPRn, the priority of interrupt number 4*n+3, or RES0 if the PE does not implement this interrupt
        field PRI_N2: u32 bit[20..23]  // For register NVIC_IPRn, the priority of interrupt number 4*n+2, or RES0 if the PE does not implement this interrupt
        field PRI_N1: u32 bit[12..15]  // For register NVIC_IPRn, the priority of interrupt number 4*n+1, or RES0 if the PE does not implement this interrupt
        field PRI_N0: u32 bit[4..7]  // For register NVIC_IPRn, the priority of interrupt number 4*n+0, or RES0 if the PE does not implement this interrupt
    }
    // Provides identification information for the PE, including an implementer code for the device and a device ID number
    reg CPUID offset 0xED00 {
        field IMPLEMENTER: u32 bit[24..31] readonly  // This field must hold an implementer code that has been assigned by ARM
        field VARIANT: u32 bit[20..23] readonly  // IMPLEMENTATION DEFINED variant number. Typically, this field is used to distinguish between different product variants, or major revisions of a product
        field ARCHITECTURE: u32 bit[16..19] readonly  // Defines the Architecture implemented by the PE
        field PARTNO: u32 bit[4..15] readonly  // IMPLEMENTATION DEFINED primary part number for the device
        field REVISION: u32 bit[0..3] readonly  // IMPLEMENTATION DEFINED revision number for the device
    }
    // Controls and provides status information for NMI, PendSV, SysTick and interrupts
    reg ICSR offset 0xED04 {
        field PENDNMISET: b1 bit[31] readonly  // Indicates whether the NMI exception is pending
        field PENDNMICLR: b1 bit[30]  // Allows the NMI exception pend state to be cleared
        field PENDSVSET: b1 bit[28] readonly  // Indicates whether the PendSV `FTSSS exception is pending
        field PENDSVCLR: b1 bit[27]  // Allows the PendSV exception pend state to be cleared `FTSSS
        field PENDSTSET: b1 bit[26] readonly  // Indicates whether the SysTick `FTSSS exception is pending
        field PENDSTCLR: b1 bit[25]  // Allows the SysTick exception pend state to be cleared `FTSSS
        field STTNS: b1 bit[24]  // Controls whether in a single SysTick implementation, the SysTick is Secure or Non-secure
        field ISRPREEMPT: b1 bit[23] readonly  // Indicates whether a pending exception will be serviced on exit from debug halt state
        field ISRPENDING: b1 bit[22] readonly  // Indicates whether an external interrupt, generated by the NVIC, is pending
        field VECTPENDING: u32 bit[12..20] readonly  // The exception number of the highest priority pending and enabled interrupt
        field RETTOBASE: b1 bit[11] readonly  // In Handler mode, indicates whether there is more than one active exception
        field VECTACTIVE: u32 bit[0..8] readonly  // The exception number of the current executing exception
    }
    // The VTOR indicates the offset of the vector table base address from memory address 0x00000000.
    reg VTOR offset 0xED08 {
        field TBLOFF: u32 bit[7..31]  // Vector table base offset field. It contains bits[31:7] of the offset of the table base from the bottom of the memory map.
    }
    // Use the Application Interrupt and Reset Control Register to: determine data endianness, clear all active state information from debug halt mode, request a system reset.
    reg AIRCR offset 0xED0C {
        field VECTKEY: u32 bit[16..31]  // Register key: 
        field ENDIANESS: b1 bit[15] readonly  // Data endianness implemented: 
        field PRIS: b1 bit[14]  // Prioritize Secure exceptions. The value of this bit defines whether Secure exception priority boosting is enabled. 
        field BFHFNMINS: b1 bit[13]  // BusFault, HardFault, and NMI Non-secure enable. 
        field PRIGROUP: u32 bit[8..10]  // Interrupt priority grouping field. This field determines the split of group priority from subpriority. 
        field SYSRESETREQS: b1 bit[3]  // System reset request, Secure state only. 
        field SYSRESETREQ: b1 bit[2]  // Writing 1 to this bit causes the SYSRESETREQ signal to the outer system to be asserted to request a reset. The intention is to force a large system reset of all major components except for debug. The C_HALT bit in the DHCSR is cleared as a result of the system reset requested. The debugger does not lose contact with the device.
        field VECTCLRACTIVE: b1 bit[1]  // Clears all active state information for fixed and configurable exceptions. This bit: is self-clearing, can only be set by the DAP when the core is halted.  When set: clears all active exception status of the processor, forces a return to Thread mode, forces an IPSR of 0. A debugger must re-initialize the stack.
    }
    // System Control Register. Use the System Control Register for power-management functions: signal to the system when the processor can enter a low power state, control how the processor enters and exits low power states.
    reg SCR offset 0xED10 {
        field SEVONPEND: b1 bit[4]  // Send Event on Pending bit: 
        field SLEEPDEEPS: b1 bit[3]  // 0 SLEEPDEEP is available to both security states 
        field SLEEPDEEP: b1 bit[2]  // Controls whether the processor uses sleep or deep sleep as its low power mode: 
        field SLEEPONEXIT: b1 bit[1]  // Indicates sleep-on-exit when returning from Handler mode to Thread mode: 
    }
    // Sets or returns configuration and control data
    reg CCR offset 0xED14 {
        field BP: b1 bit[18] readonly  // Enables program flow prediction `FTSSS
        field IC: b1 bit[17] readonly  // This is a global enable bit for instruction caches in the selected Security state
        field DC: b1 bit[16] readonly  // Enables data caching of all data accesses to Normal memory `FTSSS
        field STKOFHFNMIGN: b1 bit[10]  // Controls the effect of a stack limit violation while executing at a requested priority less than 0
        field RES1: b1 bit[9] readonly  // Reserved, RES1
        field BFHFNMIGN: b1 bit[8]  // Determines the effect of precise BusFaults on handlers running at a requested priority less than 0
        field DIV_0_TRP: b1 bit[4]  // Controls the generation of a DIVBYZERO UsageFault when attempting to perform integer division by zero
        field UNALIGN_TRP: b1 bit[3]  // Controls the trapping of unaligned word or halfword accesses
        field USERSETMPEND: b1 bit[1]  // Determines whether unprivileged accesses are permitted to pend interrupts via the STIR
        field RES1_1: b1 bit[0] readonly  // Reserved, RES1
    }
    // Sets or returns priority for system handlers 4 - 7
    reg SHPR1 offset 0xED18 {
        field PRI_7_3: u32 bit[29..31]  // Priority of system handler 7, SecureFault
        field PRI_6_3: u32 bit[21..23]  // Priority of system handler 6, SecureFault
        field PRI_5_3: u32 bit[13..15]  // Priority of system handler 5, SecureFault
        field PRI_4_3: u32 bit[5..7]  // Priority of system handler 4, SecureFault
    }
    // Sets or returns priority for system handlers 8 - 11
    reg SHPR2 offset 0xED1C {
        field PRI_11_3: u32 bit[29..31]  // Priority of system handler 11, SecureFault
        field PRI_10: u32 bit[16..23] readonly  // Reserved, RES0
        field PRI_9: u32 bit[8..15] readonly  // Reserved, RES0
        field PRI_8: u32 bit[0..7] readonly  // Reserved, RES0
    }
    // Sets or returns priority for system handlers 12 - 15
    reg SHPR3 offset 0xED20 {
        field PRI_15_3: u32 bit[29..31]  // Priority of system handler 15, SecureFault
        field PRI_14_3: u32 bit[21..23]  // Priority of system handler 14, SecureFault
        field PRI_13: u32 bit[8..15] readonly  // Reserved, RES0
        field PRI_12_3: u32 bit[5..7]  // Priority of system handler 12, SecureFault
    }
    // Provides access to the active and pending status of system exceptions
    reg SHCSR offset 0xED24 {
        field HARDFAULTPENDED: b1 bit[21]  // `IAAMO the pending state of the HardFault exception `CTTSSS
        field SECUREFAULTPENDED: b1 bit[20]  // `IAAMO the pending state of the SecureFault exception
        field SECUREFAULTENA: b1 bit[19]  // `DW the SecureFault exception is enabled
        field USGFAULTENA: b1 bit[18]  // `DW the UsageFault exception is enabled `FTSSS
        field BUSFAULTENA: b1 bit[17]  // `DW the BusFault exception is enabled
        field MEMFAULTENA: b1 bit[16]  // `DW the MemManage exception is enabled `FTSSS
        field SVCALLPENDED: b1 bit[15]  // `IAAMO the pending state of the SVCall exception `FTSSS
        field BUSFAULTPENDED: b1 bit[14]  // `IAAMO the pending state of the BusFault exception
        field MEMFAULTPENDED: b1 bit[13]  // `IAAMO the pending state of the MemManage exception `FTSSS
        field USGFAULTPENDED: b1 bit[12]  // The UsageFault exception is banked between Security states, `IAAMO the pending state of the UsageFault exception `FTSSS
        field SYSTICKACT: b1 bit[11]  // `IAAMO the active state of the SysTick exception `FTSSS
        field PENDSVACT: b1 bit[10]  // `IAAMO the active state of the PendSV exception `FTSSS
        field MONITORACT: b1 bit[8]  // `IAAMO the active state of the DebugMonitor exception
        field SVCALLACT: b1 bit[7]  // `IAAMO the active state of the SVCall exception `FTSSS
        field NMIACT: b1 bit[5]  // `IAAMO the active state of the NMI exception
        field SECUREFAULTACT: b1 bit[4]  // `IAAMO the active state of the SecureFault exception
        field USGFAULTACT: b1 bit[3]  // `IAAMO the active state of the UsageFault exception `FTSSS
        field HARDFAULTACT: b1 bit[2]  // Indicates and allows limited modification of the active state of the HardFault exception `FTSSS
        field BUSFAULTACT: b1 bit[1]  // `IAAMO the active state of the BusFault exception
        field MEMFAULTACT: b1 bit[0]  // `IAAMO the active state of the MemManage exception `FTSSS
    }
    // Contains the three Configurable Fault Status Registers.
    reg CFSR offset 0xED28 {
        field UFSR_DIVBYZERO: b1 bit[25]  // Sticky flag indicating whether an integer division by zero error has occurred
        field UFSR_UNALIGNED: b1 bit[24]  // Sticky flag indicating whether an unaligned access error has occurred
        field UFSR_STKOF: b1 bit[20]  // Sticky flag indicating whether a stack overflow error has occurred
        field UFSR_NOCP: b1 bit[19]  // Sticky flag indicating whether a coprocessor disabled or not present error has occurred
        field UFSR_INVPC: b1 bit[18]  // Sticky flag indicating whether an integrity check error has occurred
        field UFSR_INVSTATE: b1 bit[17]  // Sticky flag indicating whether an EPSR.T or EPSR.IT validity error has occurred
        field UFSR_UNDEFINSTR: b1 bit[16]  // Sticky flag indicating whether an undefined instruction error has occurred
        field BFSR_BFARVALID: b1 bit[15]  // Indicates validity of the contents of the BFAR register
        field BFSR_LSPERR: b1 bit[13]  // Records whether a BusFault occurred during FP lazy state preservation
        field BFSR_STKERR: b1 bit[12]  // Records whether a derived BusFault occurred during exception entry stacking
        field BFSR_UNSTKERR: b1 bit[11]  // Records whether a derived BusFault occurred during exception return unstacking
        field BFSR_IMPRECISERR: b1 bit[10]  // Records whether an imprecise data access error has occurred
        field BFSR_PRECISERR: b1 bit[9]  // Records whether a precise data access error has occurred
        field BFSR_IBUSERR: b1 bit[8]  // Records whether a BusFault on an instruction prefetch has occurred
        field MMFSR: u32 bit[0..7]  // Provides information on MemManage exceptions
    }
    // Shows the cause of any HardFaults
    reg HFSR offset 0xED2C {
        field DEBUGEVT: b1 bit[31]  // Indicates when a Debug event has occurred
        field FORCED: b1 bit[30]  // Indicates that a fault with configurable priority has been escalated to a HardFault exception, because it could not be made active, because of priority, or because it was disabled
        field VECTTBL: b1 bit[1]  // Indicates when a fault has occurred because of a vector table read error on exception processing
    }
    // Shows which debug event occurred
    reg DFSR offset 0xED30 {
        field EXTERNAL: b1 bit[4]  // Sticky flag indicating whether an External debug request debug event has occurred
        field VCATCH: b1 bit[3]  // Sticky flag indicating whether a Vector catch debug event has occurred
        field DWTTRAP: b1 bit[2]  // Sticky flag indicating whether a Watchpoint debug event has occurred
        field BKPT: b1 bit[1]  // Sticky flag indicating whether a Breakpoint debug event has occurred
        field HALTED: b1 bit[0]  // Sticky flag indicating that a Halt request debug event or Step debug event has occurred
    }
    // Shows the address of the memory location that caused an MPU fault
    reg MMFAR offset 0xED34 {
        field ADDRESS: u32 bit[0..31]  // This register is updated with the address of a location that produced a MemManage fault. The MMFSR shows the cause of the fault, and whether this field is valid. This field is valid only when MMFSR.MMARVALID is set, otherwise it is UNKNOWN
    }
    // Shows the address associated with a precise data access BusFault
    reg BFAR offset 0xED38 {
        field ADDRESS: u32 bit[0..31]  // This register is updated with the address of a location that produced a BusFault. The BFSR shows the reason for the fault. This field is valid only when BFSR.BFARVALID is set, otherwise it is UNKNOWN
    }
    // Gives top-level information about the instruction set supported by the PE
    reg ID_PFR0 offset 0xED40 {
        field STATE1: u32 bit[4..7] readonly  // T32 instruction set support
        field STATE0: u32 bit[0..3] readonly  // A32 instruction set support
    }
    // Gives information about the programmers' model and Extensions support
    reg ID_PFR1 offset 0xED44 {
        field MPROGMOD: u32 bit[8..11] readonly  // Identifies support for the M-Profile programmers' model support
        field SECURITY: u32 bit[4..7] readonly  // Identifies whether the Security Extension is implemented
    }
    // Provides top level information about the debug system
    reg ID_DFR0 offset 0xED48 {
        field MPROFDBG: u32 bit[20..23] readonly  // Indicates the supported M-profile debug architecture
    }
    // Provides information about the IMPLEMENTATION DEFINED features of the PE
    reg ID_AFR0 offset 0xED4C {
        field IMPDEF3: u32 bit[12..15] readonly  // IMPLEMENTATION DEFINED meaning
        field IMPDEF2: u32 bit[8..11] readonly  // IMPLEMENTATION DEFINED meaning
        field IMPDEF1: u32 bit[4..7] readonly  // IMPLEMENTATION DEFINED meaning
        field IMPDEF0: u32 bit[0..3] readonly  // IMPLEMENTATION DEFINED meaning
    }
    // Provides information about the implemented memory model and memory management support
    reg ID_MMFR0 offset 0xED50 {
        field AUXREG: u32 bit[20..23] readonly  // Indicates support for Auxiliary Control Registers
        field TCM: u32 bit[16..19] readonly  // Indicates support for tightly coupled memories (TCMs)
        field SHARELVL: u32 bit[12..15] readonly  // Indicates the number of shareability levels implemented
        field OUTERSHR: u32 bit[8..11] readonly  // Indicates the outermost shareability domain implemented
        field PMSA: u32 bit[4..7] readonly  // Indicates support for the protected memory system architecture (PMSA)
    }
    // Provides information about the implemented memory model and memory management support
    reg ID_MMFR1 offset 0xED54 {
        field ID_MMFR1: u32 bit[0..31]
    }
    // Provides information about the implemented memory model and memory management support
    reg ID_MMFR2 offset 0xED58 {
        field WFISTALL: u32 bit[24..27] readonly  // Indicates the support for Wait For Interrupt (WFI) stalling
    }
    // Provides information about the implemented memory model and memory management support
    reg ID_MMFR3 offset 0xED5C {
        field BPMAINT: u32 bit[8..11] readonly  // Indicates the supported branch predictor maintenance
        field CMAINTSW: u32 bit[4..7] readonly  // Indicates the supported cache maintenance operations by set/way
        field CMAINTVA: u32 bit[0..3] readonly  // Indicates the supported cache maintenance operations by address
    }
    // Provides information about the instruction set implemented by the PE
    reg ID_ISAR0 offset 0xED60 {
        field DIVIDE: u32 bit[24..27] readonly  // Indicates the supported Divide instructions
        field DEBUG: u32 bit[20..23] readonly  // Indicates the implemented Debug instructions
        field COPROC: u32 bit[16..19] readonly  // Indicates the supported Coprocessor instructions
        field CMPBRANCH: u32 bit[12..15] readonly  // Indicates the supported combined Compare and Branch instructions
        field BITFIELD: u32 bit[8..11] readonly  // Indicates the supported bit field instructions
        field BITCOUNT: u32 bit[4..7] readonly  // Indicates the supported bit count instructions
    }
    // Provides information about the instruction set implemented by the PE
    reg ID_ISAR1 offset 0xED64 {
        field INTERWORK: u32 bit[24..27] readonly  // Indicates the implemented Interworking instructions
        field IMMEDIATE: u32 bit[20..23] readonly  // Indicates the implemented for data-processing instructions with long immediates
        field IFTHEN: u32 bit[16..19] readonly  // Indicates the implemented If-Then instructions
        field EXTEND: u32 bit[12..15] readonly  // Indicates the implemented Extend instructions
    }
    // Provides information about the instruction set implemented by the PE
    reg ID_ISAR2 offset 0xED68 {
        field REVERSAL: u32 bit[28..31] readonly  // Indicates the implemented Reversal instructions
        field MULTU: u32 bit[20..23] readonly  // Indicates the implemented advanced unsigned Multiply instructions
        field MULTS: u32 bit[16..19] readonly  // Indicates the implemented advanced signed Multiply instructions
        field MULT: u32 bit[12..15] readonly  // Indicates the implemented additional Multiply instructions
        field MULTIACCESSINT: u32 bit[8..11] readonly  // Indicates the support for interruptible multi-access instructions
        field MEMHINT: u32 bit[4..7] readonly  // Indicates the implemented Memory Hint instructions
        field LOADSTORE: u32 bit[0..3] readonly  // Indicates the implemented additional load/store instructions
    }
    // Provides information about the instruction set implemented by the PE
    reg ID_ISAR3 offset 0xED6C {
        field TRUENOP: u32 bit[24..27] readonly  // Indicates the implemented true NOP instructions
        field T32COPY: u32 bit[20..23] readonly  // Indicates the support for T32 non flag-setting MOV instructions
        field TABBRANCH: u32 bit[16..19] readonly  // Indicates the implemented Table Branch instructions
        field SYNCHPRIM: u32 bit[12..15] readonly  // Used in conjunction with ID_ISAR4.SynchPrim_frac to indicate the implemented Synchronization Primitive instructions
        field SVC: u32 bit[8..11] readonly  // Indicates the implemented SVC instructions
        field SIMD: u32 bit[4..7] readonly  // Indicates the implemented SIMD instructions
        field SATURATE: u32 bit[0..3] readonly  // Indicates the implemented saturating instructions
    }
    // Provides information about the instruction set implemented by the PE
    reg ID_ISAR4 offset 0xED70 {
        field PSR_M: u32 bit[24..27] readonly  // Indicates the implemented M profile instructions to modify the PSRs
        field SYNCPRIM_FRAC: u32 bit[20..23] readonly  // Used in conjunction with ID_ISAR3.SynchPrim to indicate the implemented Synchronization Primitive instructions
        field BARRIER: u32 bit[16..19] readonly  // Indicates the implemented Barrier instructions
        field WRITEBACK: u32 bit[8..11] readonly  // Indicates the support for writeback addressing modes
        field WITHSHIFTS: u32 bit[4..7] readonly  // Indicates the support for writeback addressing modes
        field UNPRIV: u32 bit[0..3] readonly  // Indicates the implemented unprivileged instructions
    }
    // Provides information about the instruction set implemented by the PE
    reg ID_ISAR5 offset 0xED74 {
        field ID_ISAR5: u32 bit[0..31]
    }
    // Provides information about the architecture of the caches. CTR is RES0 if CLIDR is zero.
    reg CTR offset 0xED7C {
        field RES1: b1 bit[31] readonly  // Reserved, RES1
        field CWG: u32 bit[24..27] readonly  // Log2 of the number of words of the maximum size of memory that can be overwritten as a result of the eviction of a cache entry that has had a memory location in it modified
        field ERG: u32 bit[20..23] readonly  // Log2 of the number of words of the maximum size of the reservation granule that has been implemented for the Load-Exclusive and Store-Exclusive instructions
        field DMINLINE: u32 bit[16..19] readonly  // Log2 of the number of words in the smallest cache line of all the data caches and unified caches that are controlled by the PE
        field RES1_1: u32 bit[14..15] readonly  // Reserved, RES1
        field IMINLINE: u32 bit[0..3] readonly  // Log2 of the number of words in the smallest cache line of all the instruction caches that are controlled by the PE
    }
    // Specifies the access privileges for coprocessors and the FP Extension
    reg CPACR offset 0xED88 {
        field CP11: u32 bit[22..23]  // The value in this field is ignored. If the implementation does not include the FP Extension, this field is RAZ/WI. If the value of this bit is not programmed to the same value as the CP10 field, then the value is UNKNOWN
        field CP10: u32 bit[20..21]  // Defines the access rights for the floating-point functionality
        field CP7: u32 bit[14..15]  // Controls access privileges for coprocessor 7
        field CP6: u32 bit[12..13]  // Controls access privileges for coprocessor 6
        field CP5: u32 bit[10..11]  // Controls access privileges for coprocessor 5
        field CP4: u32 bit[8..9]  // Controls access privileges for coprocessor 4
        field CP3: u32 bit[6..7]  // Controls access privileges for coprocessor 3
        field CP2: u32 bit[4..5]  // Controls access privileges for coprocessor 2
        field CP1: u32 bit[2..3]  // Controls access privileges for coprocessor 1
        field CP0: u32 bit[0..1]  // Controls access privileges for coprocessor 0
    }
    // Defines the Non-secure access permissions for both the FP Extension and coprocessors CP0 to CP7
    reg NSACR offset 0xED8C {
        field CP11: b1 bit[11]  // Enables Non-secure access to the Floating-point Extension
        field CP10: b1 bit[10]  // Enables Non-secure access to the Floating-point Extension
        field CP7: b1 bit[7]  // Enables Non-secure access to coprocessor CP7
        field CP6: b1 bit[6]  // Enables Non-secure access to coprocessor CP6
        field CP5: b1 bit[5]  // Enables Non-secure access to coprocessor CP5
        field CP4: b1 bit[4]  // Enables Non-secure access to coprocessor CP4
        field CP3: b1 bit[3]  // Enables Non-secure access to coprocessor CP3
        field CP2: b1 bit[2]  // Enables Non-secure access to coprocessor CP2
        field CP1: b1 bit[1]  // Enables Non-secure access to coprocessor CP1
        field CP0: b1 bit[0]  // Enables Non-secure access to coprocessor CP0
    }
    // The MPU Type Register indicates how many regions the MPU `FTSSS supports
    reg MPU_TYPE offset 0xED90 {
        field DREGION: u32 bit[8..15] readonly  // Number of regions supported by the MPU
        field SEPARATE: b1 bit[0] readonly  // Indicates support for separate instructions and data address regions
    }
    // Enables the MPU and, when the MPU is enabled, controls whether the default memory map is enabled as a background region for privileged accesses, and whether the MPU is enabled for HardFaults, NMIs, and exception handlers when FAULTMASK is set to 1
    reg MPU_CTRL offset 0xED94 {
        field PRIVDEFENA: b1 bit[2]  // Controls whether the default memory map is enabled for privileged software
        field HFNMIENA: b1 bit[1]  // Controls whether handlers executing with priority less than 0 access memory with the MPU enabled or disabled. This applies to HardFaults, NMIs, and exception handlers when FAULTMASK is set to 1
        field ENABLE: b1 bit[0]  // Enables the MPU
    }
    // Selects the region currently accessed by MPU_RBAR and MPU_RLAR
    reg MPU_RNR offset 0xED98 {
        field REGION: u32 bit[0..2]  // Indicates the memory region accessed by MPU_RBAR and MPU_RLAR
    }
    // Provides indirect read and write access to the base address of the currently selected MPU region `FTSSS
    reg MPU_RBAR offset 0xED9C {
        field BASE: u32 bit[5..31]  // Contains bits [31:5] of the lower inclusive limit of the selected MPU memory region. This value is zero extended to provide the base address to be checked against
        field SH: u32 bit[3..4]  // Defines the Shareability domain of this region for Normal memory
        field AP: u32 bit[1..2]  // Defines the access permissions for this region
        field XN: b1 bit[0]  // Defines whether code can be executed from this region
    }
    // Provides indirect read and write access to the limit address of the currently selected MPU region `FTSSS
    reg MPU_RLAR offset 0xEDA0 {
        field LIMIT: u32 bit[5..31]  // Contains bits [31:5] of the upper inclusive limit of the selected MPU memory region. This value is postfixed with 0x1F to provide the limit address to be checked against
        field ATTRINDX: u32 bit[1..3]  // Associates a set of attributes in the MPU_MAIR0 and MPU_MAIR1 fields
        field EN: b1 bit[0]  // Region enable
    }
    // Provides indirect read and write access to the base address of the MPU region selected by MPU_RNR[7:2]:(1[1:0]) `FTSSS
    reg MPU_RBAR_A1 offset 0xEDA4 {
        field BASE: u32 bit[5..31]  // Contains bits [31:5] of the lower inclusive limit of the selected MPU memory region. This value is zero extended to provide the base address to be checked against
        field SH: u32 bit[3..4]  // Defines the Shareability domain of this region for Normal memory
        field AP: u32 bit[1..2]  // Defines the access permissions for this region
        field XN: b1 bit[0]  // Defines whether code can be executed from this region
    }
    // Provides indirect read and write access to the limit address of the currently selected MPU region selected by MPU_RNR[7:2]:(1[1:0]) `FTSSS
    reg MPU_RLAR_A1 offset 0xEDA8 {
        field LIMIT: u32 bit[5..31]  // Contains bits [31:5] of the upper inclusive limit of the selected MPU memory region. This value is postfixed with 0x1F to provide the limit address to be checked against
        field ATTRINDX: u32 bit[1..3]  // Associates a set of attributes in the MPU_MAIR0 and MPU_MAIR1 fields
        field EN: b1 bit[0]  // Region enable
    }
    // Provides indirect read and write access to the base address of the MPU region selected by MPU_RNR[7:2]:(2[1:0]) `FTSSS
    reg MPU_RBAR_A2 offset 0xEDAC {
        field BASE: u32 bit[5..31]  // Contains bits [31:5] of the lower inclusive limit of the selected MPU memory region. This value is zero extended to provide the base address to be checked against
        field SH: u32 bit[3..4]  // Defines the Shareability domain of this region for Normal memory
        field AP: u32 bit[1..2]  // Defines the access permissions for this region
        field XN: b1 bit[0]  // Defines whether code can be executed from this region
    }
    // Provides indirect read and write access to the limit address of the currently selected MPU region selected by MPU_RNR[7:2]:(2[1:0]) `FTSSS
    reg MPU_RLAR_A2 offset 0xEDB0 {
        field LIMIT: u32 bit[5..31]  // Contains bits [31:5] of the upper inclusive limit of the selected MPU memory region. This value is postfixed with 0x1F to provide the limit address to be checked against
        field ATTRINDX: u32 bit[1..3]  // Associates a set of attributes in the MPU_MAIR0 and MPU_MAIR1 fields
        field EN: b1 bit[0]  // Region enable
    }
    // Provides indirect read and write access to the base address of the MPU region selected by MPU_RNR[7:2]:(3[1:0]) `FTSSS
    reg MPU_RBAR_A3 offset 0xEDB4 {
        field BASE: u32 bit[5..31]  // Contains bits [31:5] of the lower inclusive limit of the selected MPU memory region. This value is zero extended to provide the base address to be checked against
        field SH: u32 bit[3..4]  // Defines the Shareability domain of this region for Normal memory
        field AP: u32 bit[1..2]  // Defines the access permissions for this region
        field XN: b1 bit[0]  // Defines whether code can be executed from this region
    }
    // Provides indirect read and write access to the limit address of the currently selected MPU region selected by MPU_RNR[7:2]:(3[1:0]) `FTSSS
    reg MPU_RLAR_A3 offset 0xEDB8 {
        field LIMIT: u32 bit[5..31]  // Contains bits [31:5] of the upper inclusive limit of the selected MPU memory region. This value is postfixed with 0x1F to provide the limit address to be checked against
        field ATTRINDX: u32 bit[1..3]  // Associates a set of attributes in the MPU_MAIR0 and MPU_MAIR1 fields
        field EN: b1 bit[0]  // Region enable
    }
    // Along with MPU_MAIR1, provides the memory attribute encodings corresponding to the AttrIndex values
    reg MPU_MAIR0 offset 0xEDC0 {
        field ATTR3: u32 bit[24..31]  // Memory attribute encoding for MPU regions with an AttrIndex of 3
        field ATTR2: u32 bit[16..23]  // Memory attribute encoding for MPU regions with an AttrIndex of 2
        field ATTR1: u32 bit[8..15]  // Memory attribute encoding for MPU regions with an AttrIndex of 1
        field ATTR0: u32 bit[0..7]  // Memory attribute encoding for MPU regions with an AttrIndex of 0
    }
    // Along with MPU_MAIR0, provides the memory attribute encodings corresponding to the AttrIndex values
    reg MPU_MAIR1 offset 0xEDC4 {
        field ATTR7: u32 bit[24..31]  // Memory attribute encoding for MPU regions with an AttrIndex of 7
        field ATTR6: u32 bit[16..23]  // Memory attribute encoding for MPU regions with an AttrIndex of 6
        field ATTR5: u32 bit[8..15]  // Memory attribute encoding for MPU regions with an AttrIndex of 5
        field ATTR4: u32 bit[0..7]  // Memory attribute encoding for MPU regions with an AttrIndex of 4
    }
    // Allows enabling of the Security Attribution Unit
    reg SAU_CTRL offset 0xEDD0 {
        field ALLNS: b1 bit[1]  // When SAU_CTRL.ENABLE is 0 this bit controls if the memory is marked as Non-secure or Secure
        field ENABLE: b1 bit[0]  // Enables the SAU
    }
    // Indicates the number of regions implemented by the Security Attribution Unit
    reg SAU_TYPE offset 0xEDD4 {
        field SREGION: u32 bit[0..7] readonly  // The number of implemented SAU regions
    }
    // Selects the region currently accessed by SAU_RBAR and SAU_RLAR
    reg SAU_RNR offset 0xEDD8 {
        field REGION: u32 bit[0..7]  // Indicates the SAU region accessed by SAU_RBAR and SAU_RLAR
    }
    // Provides indirect read and write access to the base address of the currently selected SAU region
    reg SAU_RBAR offset 0xEDDC {
        field BADDR: u32 bit[5..31]  // Holds bits [31:5] of the base address for the selected SAU region
    }
    // Provides indirect read and write access to the limit address of the currently selected SAU region
    reg SAU_RLAR offset 0xEDE0 {
        field LADDR: u32 bit[5..31]  // Holds bits [31:5] of the limit address for the selected SAU region
        field NSC: b1 bit[1]  // Controls whether Non-secure state is permitted to execute an SG instruction from this region
        field ENABLE: b1 bit[0]  // SAU region enable
    }
    // Provides information about any security related faults
    reg SFSR offset 0xEDE4 {
        field LSERR: b1 bit[7]  // Sticky flag indicating that an error occurred during lazy state activation or deactivation
        field SFARVALID: b1 bit[6]  // This bit is set when the SFAR register contains a valid value. As with similar fields, such as BFSR.BFARVALID and MMFSR.MMARVALID, this bit can be cleared by other exceptions, such as BusFault
        field LSPERR: b1 bit[5]  // Stick flag indicating that an SAU or IDAU violation occurred during the lazy preservation of floating-point state
        field INVTRAN: b1 bit[4]  // Sticky flag indicating that an exception was raised due to a branch that was not flagged as being domain crossing causing a transition from Secure to Non-secure memory
        field AUVIOL: b1 bit[3]  // Sticky flag indicating that an attempt was made to access parts of the address space that are marked as Secure with NS-Req for the transaction set to Non-secure. This bit is not set if the violation occurred during lazy state preservation. See LSPERR
        field INVER: b1 bit[2]  // This can be caused by EXC_RETURN.DCRS being set to 0 when returning from an exception in the Non-secure state, or by EXC_RETURN.ES being set to 1 when returning from an exception in the Non-secure state
        field INVIS: b1 bit[1]  // This bit is set if the integrity signature in an exception stack frame is found to be invalid during the unstacking operation
        field INVEP: b1 bit[0]  // This bit is set if a function call from the Non-secure state or exception targets a non-SG instruction in the Secure state. This bit is also set if the target address is a SG instruction, but there is no matching SAU/IDAU region with the NSC flag set
    }
    // Shows the address of the memory location that caused a Security violation
    reg SFAR offset 0xEDE8 {
        field ADDRESS: u32 bit[0..31]  // The address of an access that caused a attribution unit violation. This field is only valid when SFSR.SFARVALID is set. This allows the actual flip flops associated with this register to be shared with other fault address registers. If an implementation chooses to share the storage in this way, care must be taken to not leak Secure address information to the Non-secure state. One way of achieving this is to share the SFAR register with the MMFAR_S register, which is not accessible to the Non-secure state
    }
    // Controls halting debug
    reg DHCSR offset 0xEDF0 {
        field S_RESTART_ST: b1 bit[26] readonly  // Indicates the PE has processed a request to clear DHCSR.C_HALT to 0. That is, either a write to DHCSR that clears DHCSR.C_HALT from 1 to 0, or an External Restart Request
        field S_RESET_ST: b1 bit[25] readonly  // Indicates whether the PE has been reset since the last read of the DHCSR
        field S_RETIRE_ST: b1 bit[24] readonly  // Set to 1 every time the PE retires one of more instructions
        field S_SDE: b1 bit[20] readonly  // Indicates whether Secure invasive debug is allowed
        field S_LOCKUP: b1 bit[19] readonly  // Indicates whether the PE is in Lockup state
        field S_SLEEP: b1 bit[18] readonly  // Indicates whether the PE is sleeping
        field S_HALT: b1 bit[17] readonly  // Indicates whether the PE is in Debug state
        field S_REGRDY: b1 bit[16] readonly  // Handshake flag to transfers through the DCRDR
        field C_SNAPSTALL: b1 bit[5]  // Allow imprecise entry to Debug state
        field C_MASKINTS: b1 bit[3]  // When debug is enabled, the debugger can write to this bit to mask PendSV, SysTick and external configurable interrupts
        field C_STEP: b1 bit[2]  // Enable single instruction step
        field C_HALT: b1 bit[1]  // PE enter Debug state halt request
        field C_DEBUGEN: b1 bit[0]  // Enable Halting debug
    }
    // With the DCRDR, provides debug access to the general-purpose registers, special-purpose registers, and the FP extension registers. A write to the DCRSR specifies the register to transfer, whether the transfer is a read or write, and starts the transfer
    reg DCRSR offset 0xEDF4 {
        field REGWNR: b1 bit[16]  // Specifies the access type for the transfer
        field REGSEL: u32 bit[0..6]  // Specifies the general-purpose register, special-purpose register, or FP register to transfer
    }
    // With the DCRSR, provides debug access to the general-purpose registers, special-purpose registers, and the FP Extension registers. If the Main Extension is implemented, it can also be used for message passing between an external debugger and a debug agent running on the PE
    reg DCRDR offset 0xEDF8 {
        field DBGTMP: u32 bit[0..31]  // Provides debug access for reading and writing the general-purpose registers, special-purpose registers, and Floating-point Extension registers
    }
    // Manages vector catch behavior and DebugMonitor handling when debugging
    reg DEMCR offset 0xEDFC {
        field TRCENA: b1 bit[24]  // Global enable for all DWT and ITM features
        field SDME: b1 bit[20] readonly  // Indicates whether the DebugMonitor targets the Secure or the Non-secure state and whether debug events are allowed in Secure state
        field MON_REQ: b1 bit[19]  // DebugMonitor semaphore bit
        field MON_STEP: b1 bit[18]  // Enable DebugMonitor stepping
        field MON_PEND: b1 bit[17]  // Sets or clears the pending state of the DebugMonitor exception
        field MON_EN: b1 bit[16]  // Enable the DebugMonitor exception
        field VC_SFERR: b1 bit[11]  // SecureFault exception halting debug vector catch enable
        field VC_HARDERR: b1 bit[10]  // HardFault exception halting debug vector catch enable
        field VC_INTERR: b1 bit[9]  // Enable halting debug vector catch for faults during exception entry and return
        field VC_BUSERR: b1 bit[8]  // BusFault exception halting debug vector catch enable
        field VC_STATERR: b1 bit[7]  // Enable halting debug trap on a UsageFault exception caused by a state information error, for example an Undefined Instruction exception
        field VC_CHKERR: b1 bit[6]  // Enable halting debug trap on a UsageFault exception caused by a checking error, for example an alignment check error
        field VC_NOCPERR: b1 bit[5]  // Enable halting debug trap on a UsageFault caused by an access to a coprocessor
        field VC_MMERR: b1 bit[4]  // Enable halting debug trap on a MemManage exception
        field VC_CORERESET: b1 bit[0]  // Enable Reset Vector Catch. This causes a warm reset to halt a running system
    }
    // Provides control and status information for Secure debug
    reg DSCSR offset 0xEE08 {
        field CDSKEY: b1 bit[17]  // Writes to the CDS bit are ignored unless CDSKEY is concurrently written to zero
        field CDS: b1 bit[16]  // This field indicates the current Security state of the processor
        field SBRSEL: b1 bit[1]  // If SBRSELEN is 1 this bit selects whether the Non-secure or the Secure version of the memory-mapped Banked registers are accessible to the debugger
        field SBRSELEN: b1 bit[0]  // Controls whether the SBRSEL field or the current Security state of the processor selects which version of the memory-mapped Banked registers are accessed to the debugger
    }
    // Provides a mechanism for software to generate an interrupt
    reg STIR offset 0xEF00 {
        field INTID: u32 bit[0..8]  // Indicates the interrupt to be pended. The value written is (ExceptionNumber - 16)
    }
    // Holds control data for the Floating-point extension
    reg FPCCR offset 0xEF34 {
        field ASPEN: b1 bit[31]  // When this bit is set to 1, execution of a floating-point instruction sets the CONTROL.FPCA bit to 1
        field LSPEN: b1 bit[30]  // Enables lazy context save of floating-point state
        field LSPENS: b1 bit[29]  // This bit controls whether the LSPEN bit is writeable from the Non-secure state
        field CLRONRET: b1 bit[28]  // Clear floating-point caller saved registers on exception return
        field CLRONRETS: b1 bit[27]  // This bit controls whether the CLRONRET bit is writeable from the Non-secure state
        field TS: b1 bit[26]  // Treat floating-point registers as Secure enable
        field UFRDY: b1 bit[10]  // Indicates whether the software executing when the PE allocated the floating-point stack frame was able to set the UsageFault exception to pending
        field SPLIMVIOL: b1 bit[9]  // This bit is banked between the Security states and indicates whether the floating-point context violates the stack pointer limit that was active when lazy state preservation was activated. SPLIMVIOL modifies the lazy floating-point state preservation behavior
        field MONRDY: b1 bit[8]  // Indicates whether the software executing when the PE allocated the floating-point stack frame was able to set the DebugMonitor exception to pending
        field SFRDY: b1 bit[7]  // Indicates whether the software executing when the PE allocated the floating-point stack frame was able to set the SecureFault exception to pending. This bit is only present in the Secure version of the register, and behaves as RAZ/WI when accessed from the Non-secure state
        field BFRDY: b1 bit[6]  // Indicates whether the software executing when the PE allocated the floating-point stack frame was able to set the BusFault exception to pending
        field MMRDY: b1 bit[5]  // Indicates whether the software executing when the PE allocated the floating-point stack frame was able to set the MemManage exception to pending
        field HFRDY: b1 bit[4]  // Indicates whether the software executing when the PE allocated the floating-point stack frame was able to set the HardFault exception to pending
        field THREAD: b1 bit[3]  // Indicates the PE mode when it allocated the floating-point stack frame
        field S: b1 bit[2]  // Security status of the floating-point context. This bit is only present in the Secure version of the register, and behaves as RAZ/WI when accessed from the Non-secure state. This bit is updated whenever lazy state preservation is activated, or when a floating-point instruction is executed
        field USER: b1 bit[1]  // Indicates the privilege level of the software executing when the PE allocated the floating-point stack frame
        field LSPACT: b1 bit[0]  // Indicates whether lazy preservation of the floating-point state is active
    }
    // Holds the location of the unpopulated floating-point register space allocated on an exception stack frame
    reg FPCAR offset 0xEF38 {
        field ADDRESS: u32 bit[3..31]  // The location of the unpopulated floating-point register space allocated on an exception stack frame
    }
    // Holds the default values for the floating-point status control data that the PE assigns to the FPSCR when it creates a new floating-point context
    reg FPDSCR offset 0xEF3C {
        field AHP: b1 bit[26]  // Default value for FPSCR.AHP
        field DN: b1 bit[25]  // Default value for FPSCR.DN
        field FZ: b1 bit[24]  // Default value for FPSCR.FZ
        field RMODE: u32 bit[22..23]  // Default value for FPSCR.RMode
    }
    // Describes the features provided by the Floating-point Extension
    reg MVFR0 offset 0xEF40 {
        field FPROUND: u32 bit[28..31] readonly  // Indicates the rounding modes supported by the FP Extension
        field FPSQRT: u32 bit[20..23] readonly  // Indicates the support for FP square root operations
        field FPDIVIDE: u32 bit[16..19] readonly  // Indicates the support for FP divide operations
        field FPDP: u32 bit[8..11] readonly  // Indicates support for FP double-precision operations
        field FPSP: u32 bit[4..7] readonly  // Indicates support for FP single-precision operations
        field SIMDREG: u32 bit[0..3] readonly  // Indicates size of FP register file
    }
    // Describes the features provided by the Floating-point Extension
    reg MVFR1 offset 0xEF44 {
        field FMAC: u32 bit[28..31] readonly  // Indicates whether the FP Extension implements the fused multiply accumulate instructions
        field FPHP: u32 bit[24..27] readonly  // Indicates whether the FP Extension implements half-precision FP conversion instructions
        field FPDNAN: u32 bit[4..7] readonly  // Indicates whether the FP hardware implementation supports NaN propagation
        field FPFTZ: u32 bit[0..3] readonly  // Indicates whether subnormals are always flushed-to-zero
    }
    // Describes the features provided by the Floating-point Extension
    reg MVFR2 offset 0xEF48 {
        field FPMISC: u32 bit[4..7] readonly  // Indicates support for miscellaneous FP features
    }
    // Provides CoreSight discovery information for the SCS
    reg DDEVARCH offset 0xEFBC {
        field ARCHITECT: u32 bit[21..31] readonly  // Defines the architect of the component. Bits [31:28] are the JEP106 continuation code (JEP106 bank ID, minus 1) and bits [27:21] are the JEP106 ID code.
        field PRESENT: b1 bit[20] readonly  // Defines that the DEVARCH register is present
        field REVISION: u32 bit[16..19] readonly  // Defines the architecture revision of the component
        field ARCHVER: u32 bit[12..15] readonly  // Defines the architecture version of the component
        field ARCHPART: u32 bit[0..11] readonly  // Defines the architecture of the component
    }
    // Provides CoreSight discovery information for the SCS
    reg DDEVTYPE offset 0xEFCC {
        field SUB: u32 bit[4..7] readonly  // Component sub-type
        field MAJOR: u32 bit[0..3] readonly  // CoreSight major type
    }
    // Provides CoreSight discovery information for the SCS
    reg DPIDR4 offset 0xEFD0 {
        field SIZE: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field DES_2: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the SCS
    reg DPIDR5 offset 0xEFD4 {
        field DPIDR5: u32 bit[0..31]
    }
    // Provides CoreSight discovery information for the SCS
    reg DPIDR6 offset 0xEFD8 {
        field DPIDR6: u32 bit[0..31]
    }
    // Provides CoreSight discovery information for the SCS
    reg DPIDR7 offset 0xEFDC {
        field DPIDR7: u32 bit[0..31]
    }
    // Provides CoreSight discovery information for the SCS
    reg DPIDR0 offset 0xEFE0 {
        field PART_0: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the SCS
    reg DPIDR1 offset 0xEFE4 {
        field DES_0: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field PART_1: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the SCS
    reg DPIDR2 offset 0xEFE8 {
        field REVISION: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field JEDEC: b1 bit[3] readonly  // See CoreSight Architecture Specification
        field DES_1: u32 bit[0..2] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the SCS
    reg DPIDR3 offset 0xEFEC {
        field REVAND: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field CMOD: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the SCS
    reg DCIDR0 offset 0xEFF0 {
        field PRMBL_0: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the SCS
    reg DCIDR1 offset 0xEFF4 {
        field CLASS: u32 bit[4..7] readonly  // See CoreSight Architecture Specification
        field PRMBL_1: u32 bit[0..3] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the SCS
    reg DCIDR2 offset 0xEFF8 {
        field PRMBL_2: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Provides CoreSight discovery information for the SCS
    reg DCIDR3 offset 0xEFFC {
        field PRMBL_3: u32 bit[0..7] readonly  // See CoreSight Architecture Specification
    }
    // Programming Control Register
    reg TRCPRGCTLR offset 0x41004 {
        field EN: b1 bit[0]  // Trace Unit Enable
    }
    // The TRCSTATR indicates the ETM-Teal status
    reg TRCSTATR offset 0x4100C {
        field PMSTABLE: b1 bit[1] readonly  // Indicates whether the ETM-Teal registers are stable and can be read
        field IDLE: b1 bit[0] readonly  // Indicates that the trace unit is inactive
    }
    // The TRCCONFIGR sets the basic tracing options for the trace unit
    reg TRCCONFIGR offset 0x41010 {
        field RS: b1 bit[12]  // Return stack enable
        field TS: b1 bit[11]  // Global timestamp tracing
        field COND: u32 bit[5..10]  // Conditional instruction tracing
        field CCI: b1 bit[4]  // Cycle counting in instruction trace
        field BB: b1 bit[3]  // Branch broadcast mode
    }
    // The TRCEVENTCTL0R controls the tracing of events in the trace stream. The events also drive the ETM-Teal external outputs.
    reg TRCEVENTCTL0R offset 0x41020 {
        field TYPE1: b1 bit[15]  // Selects the resource type for event 1
        field SEL1: u32 bit[8..10]  // Selects the resource number, based on the value of TYPE1: When TYPE1 is 0, selects a single selected resource from 0-15 defined by SEL1[2:0].  When TYPE1 is 1, selects a Boolean combined resource pair from 0-7 defined by SEL1[2:0]
        field TYPE0: b1 bit[7]  // Selects the resource type for event 0
        field SEL0: u32 bit[0..2]  // Selects the resource number, based on the value of TYPE0: When TYPE1 is 0, selects a single selected resource from 0-15 defined by SEL0[2:0].  When TYPE1 is 1, selects a Boolean combined resource pair from 0-7 defined by SEL0[2:0]
    }
    // The TRCEVENTCTL1R controls how the events selected by TRCEVENTCTL0R behave
    reg TRCEVENTCTL1R offset 0x41024 {
        field LPOVERRIDE: b1 bit[12]  // Low power state behavior override
        field ATB: b1 bit[11]  // ATB enabled
        field INSTEN1: b1 bit[1]  // One bit per event, to enable generation of an event element in the instruction trace stream when the selected event occurs
        field INSTEN0: b1 bit[0]  // One bit per event, to enable generation of an event element in the instruction trace stream when the selected event occurs
    }
    // The TRCSTALLCTLR enables ETM-Teal to stall the processor if the ETM-Teal FIFO goes over the programmed level to minimize risk of overflow
    reg TRCSTALLCTLR offset 0x4102C {
        field INSTPRIORITY: b1 bit[10] readonly  // Reserved, RES0
        field ISTALL: b1 bit[8]  // Stall processor based on instruction trace buffer space
        field LEVEL: u32 bit[2..3]  // Threshold at which stalling becomes active. This provides four levels. This level can be varied to optimize the level of invasion caused by stalling, balanced against the risk of a FIFO overflow
    }
    // The TRCTSCTLR controls the insertion of global timestamps into the trace stream. A timestamp is always inserted into the instruction trace stream
    reg TRCTSCTLR offset 0x41030 {
        field TYPE0: b1 bit[7]  // Selects the resource type for event 0
        field SEL0: u32 bit[0..1]  // Selects the resource number, based on the value of TYPE0: When TYPE1 is 0, selects a single selected resource from 0-15 defined by SEL0[2:0].  When TYPE1 is 1, selects a Boolean combined resource pair from 0-7 defined by SEL0[2:0]
    }
    // The TRCSYNCPR specifies the period of trace synchronization of the trace streams. TRCSYNCPR defines a number of bytes of trace between requests for trace synchronization. This value is always a power of two
    reg TRCSYNCPR offset 0x41034 {
        field PERIOD: u32 bit[0..4] readonly  // Defines the number of bytes of trace between trace synchronization requests as a total of the number of bytes generated by the instruction stream. The number of bytes is 2N where N is the value of this field: - A value of zero disables these periodic trace synchronization requests, but does not disable other trace synchronization requests.  - The minimum value that can be programmed, other than zero, is 8, providing a minimum trace synchronization period of 256 bytes.  - The maximum value is 20, providing a maximum trace synchronization period of 2^20 bytes
    }
    // The TRCCCCTLR sets the threshold value for instruction trace cycle counting. The threshold represents the minimum interval between cycle count trace packets
    reg TRCCCCTLR offset 0x41038 {
        field THRESHOLD: u32 bit[0..11]  // Instruction trace cycle count threshold
    }
    // The TRCVICTLR controls instruction trace filtering
    reg TRCVICTLR offset 0x41080 {
        field EXLEVEL_S3: b1 bit[19]  // In Secure state, each bit controls whether instruction tracing is enabled for the corresponding exception level
        field EXLEVEL_S0: b1 bit[16]  // In Secure state, each bit controls whether instruction tracing is enabled for the corresponding exception level
        field TRCERR: b1 bit[11]  // Selects whether a system error exception must always be traced
        field TRCRESET: b1 bit[10]  // Selects whether a reset exception must always be traced
        field SSSTATUS: b1 bit[9]  // Indicates the current status of the start/stop logic
        field TYPE0: b1 bit[7]  // Selects the resource type for event 0
        field SEL0: u32 bit[0..1]  // Selects the resource number, based on the value of TYPE0: When TYPE1 is 0, selects a single selected resource from 0-15 defined by SEL0[2:0].  When TYPE1 is 1, selects a Boolean combined resource pair from 0-7 defined by SEL0[2:0]
    }
    // The TRCCNTRLDVR defines the reload value for the reduced function counter
    reg TRCCNTRLDVR0 offset 0x41140 {
        field VALUE: u32 bit[0..15]  // Defines the reload value for the counter. This value is loaded into the counter each time the reload event occurs
    }
    // TRCIDR8
    reg TRCIDR8 offset 0x41180 {
        field MAXSPEC: u32 bit[0..31] readonly  // reads as `ImpDef
    }
    // TRCIDR9
    reg TRCIDR9 offset 0x41184 {
        field NUMP0KEY: u32 bit[0..31] readonly  // reads as `ImpDef
    }
    // TRCIDR10
    reg TRCIDR10 offset 0x41188 {
        field NUMP1KEY: u32 bit[0..31] readonly  // reads as `ImpDef
    }
    // TRCIDR11
    reg TRCIDR11 offset 0x4118C {
        field NUMP1SPC: u32 bit[0..31] readonly  // reads as `ImpDef
    }
    // TRCIDR12
    reg TRCIDR12 offset 0x41190 {
        field NUMCONDKEY: u32 bit[0..31] readonly  // reads as `ImpDef
    }
    // TRCIDR13
    reg TRCIDR13 offset 0x41194 {
        field NUMCONDSPC: u32 bit[0..31] readonly  // reads as `ImpDef
    }
    // The TRCIMSPEC shows the presence of any IMPLEMENTATION SPECIFIC features, and enables any features that are provided
    reg TRCIMSPEC offset 0x411C0 {
        field SUPPORT: u32 bit[0..3] readonly  // Reserved, RES0
    }
    // TRCIDR0
    reg TRCIDR0 offset 0x411E0 {
        field COMMOPT: b1 bit[29] readonly  // reads as `ImpDef
        field TSSIZE: u32 bit[24..28] readonly  // reads as `ImpDef
        field TRCEXDATA: b1 bit[17] readonly  // reads as `ImpDef
        field QSUPP: u32 bit[15..16] readonly  // reads as `ImpDef
        field QFILT: b1 bit[14] readonly  // reads as `ImpDef
        field CONDTYPE: u32 bit[12..13] readonly  // reads as `ImpDef
        field NUMEVENT: u32 bit[10..11] readonly  // reads as `ImpDef
        field RETSTACK: b1 bit[9] readonly  // reads as `ImpDef
        field TRCCCI: b1 bit[7] readonly  // reads as `ImpDef
        field TRCCOND: b1 bit[6] readonly  // reads as `ImpDef
        field TRCBB: b1 bit[5] readonly  // reads as `ImpDef
        field TRCDATA: u32 bit[3..4] readonly  // reads as `ImpDef
        field INSTP0: u32 bit[1..2] readonly  // reads as `ImpDef
        field RES1: b1 bit[0] readonly  // Reserved, RES1
    }
    // TRCIDR1
    reg TRCIDR1 offset 0x411E4 {
        field DESIGNER: u32 bit[24..31] readonly  // reads as `ImpDef
        field RES1: u32 bit[12..15] readonly  // Reserved, RES1
        field TRCARCHMAJ: u32 bit[8..11] readonly  // reads as 0b0100
        field TRCARCHMIN: u32 bit[4..7] readonly  // reads as 0b0000
        field REVISION: u32 bit[0..3] readonly  // reads as `ImpDef
    }
    // TRCIDR2
    reg TRCIDR2 offset 0x411E8 {
        field CCSIZE: u32 bit[25..28] readonly  // reads as `ImpDef
        field DVSIZE: u32 bit[20..24] readonly  // reads as `ImpDef
        field DASIZE: u32 bit[15..19] readonly  // reads as `ImpDef
        field VMIDSIZE: u32 bit[10..14] readonly  // reads as `ImpDef
        field CIDSIZE: u32 bit[5..9] readonly  // reads as `ImpDef
        field IASIZE: u32 bit[0..4] readonly  // reads as `ImpDef
    }
    // TRCIDR3
    reg TRCIDR3 offset 0x411EC {
        field NOOVERFLOW: b1 bit[31] readonly  // reads as `ImpDef
        field NUMPROC: u32 bit[28..30] readonly  // reads as `ImpDef
        field SYSSTALL: b1 bit[27] readonly  // reads as `ImpDef
        field STALLCTL: b1 bit[26] readonly  // reads as `ImpDef
        field SYNCPR: b1 bit[25] readonly  // reads as `ImpDef
        field TRCERR: b1 bit[24] readonly  // reads as `ImpDef
        field EXLEVEL_NS: u32 bit[20..23] readonly  // reads as `ImpDef
        field EXLEVEL_S: u32 bit[16..19] readonly  // reads as `ImpDef
        field CCITMIN: u32 bit[0..11] readonly  // reads as `ImpDef
    }
    // TRCIDR4
    reg TRCIDR4 offset 0x411F0 {
        field NUMVMIDC: u32 bit[28..31] readonly  // reads as `ImpDef
        field NUMCIDC: u32 bit[24..27] readonly  // reads as `ImpDef
        field NUMSSCC: u32 bit[20..23] readonly  // reads as `ImpDef
        field NUMRSPAIR: u32 bit[16..19] readonly  // reads as `ImpDef
        field NUMPC: u32 bit[12..15] readonly  // reads as `ImpDef
        field SUPPDAC: b1 bit[8] readonly  // reads as `ImpDef
        field NUMDVC: u32 bit[4..7] readonly  // reads as `ImpDef
        field NUMACPAIRS: u32 bit[0..3] readonly  // reads as `ImpDef
    }
    // TRCIDR5
    reg TRCIDR5 offset 0x411F4 {
        field REDFUNCNTR: b1 bit[31] readonly  // reads as `ImpDef
        field NUMCNTR: u32 bit[28..30] readonly  // reads as `ImpDef
        field NUMSEQSTATE: u32 bit[25..27] readonly  // reads as `ImpDef
        field LPOVERRIDE: b1 bit[23] readonly  // reads as `ImpDef
        field ATBTRIG: b1 bit[22] readonly  // reads as `ImpDef
        field TRACEIDSIZE: u32 bit[16..21] readonly  // reads as 0x07
        field NUMEXTINSEL: u32 bit[9..11] readonly  // reads as `ImpDef
        field NUMEXTIN: u32 bit[0..8] readonly  // reads as `ImpDef
    }
    // TRCIDR6
    reg TRCIDR6 offset 0x411F8 {
        field TRCIDR6: u32 bit[0..31]
    }
    // TRCIDR7
    reg TRCIDR7 offset 0x411FC {
        field TRCIDR7: u32 bit[0..31]
    }
    // The TRCRSCTLR controls the trace resources
    reg TRCRSCTLR2 offset 0x41208 {
        field PAIRINV: b1 bit[21]  // Inverts the result of a combined pair of resources.  This bit is only implemented on the lower register for a pair of resource selectors
        field INV: b1 bit[20]  // Inverts the selected resources
        field GROUP: u32 bit[16..18]  // Selects a group of resource
        field SELECT: u32 bit[0..7]  // Selects one or more resources from the wanted group. One bit is provided per resource from the group
    }
    // The TRCRSCTLR controls the trace resources
    reg TRCRSCTLR3 offset 0x4120C {
        field PAIRINV: b1 bit[21]  // Inverts the result of a combined pair of resources.  This bit is only implemented on the lower register for a pair of resource selectors
        field INV: b1 bit[20]  // Inverts the selected resources
        field GROUP: u32 bit[16..18]  // Selects a group of resource
        field SELECT: u32 bit[0..7]  // Selects one or more resources from the wanted group. One bit is provided per resource from the group
    }
    // Controls the corresponding single-shot comparator resource
    reg TRCSSCSR offset 0x412A0 {
        field STATUS: b1 bit[31]  // Single-shot status bit. Indicates if any of the comparators, that TRCSSCCRn.SAC or TRCSSCCRn.ARC selects, have matched
        field PC: b1 bit[3] readonly  // Reserved, RES1
        field DV: b1 bit[2] readonly  // Reserved, RES0
        field DA: b1 bit[1] readonly  // Reserved, RES0
        field INST: b1 bit[0] readonly  // Reserved, RES0
    }
    // Selects the PE comparator inputs for Single-shot control
    reg TRCSSPCICR offset 0x412C0 {
        field PC: u32 bit[0..3]  // Selects one or more PE comparator inputs for Single-shot control.  TRCIDR4.NUMPC defines the size of the PC field.  1 bit is provided for each implemented PE comparator input.  For example, if bit[1] == 1 this selects PE comparator input 1 for Single-shot control
    }
    // Requests the system to provide power to the trace unit
    reg TRCPDCR offset 0x41310 {
        field PU: b1 bit[3]  // Powerup request bit:
    }
    // Returns the following information about the trace unit: - OS Lock status.  - Core power domain status.  - Power interruption status
    reg TRCPDSR offset 0x41314 {
        field OSLK: b1 bit[5] readonly  // OS Lock status bit:
        field STICKYPD: b1 bit[1] readonly  // Sticky powerdown status bit. Indicates whether the trace register state is valid:
        field POWER: b1 bit[0] readonly  // Power status bit:
    }
    // Trace Integration ATB Identification Register
    reg TRCITATBIDR offset 0x41EE4 {
        field ID: u32 bit[0..6]  // Trace ID
    }
    // Trace Integration Instruction ATB In Register
    reg TRCITIATBINR offset 0x41EF4 {
        field AFVALIDM: b1 bit[1]  // Integration Mode instruction AFVALIDM in
        field ATREADYM: b1 bit[0]  // Integration Mode instruction ATREADYM in
    }
    // Trace Integration Instruction ATB Out Register
    reg TRCITIATBOUTR offset 0x41EFC {
        field AFREADY: b1 bit[1]  // Integration Mode instruction AFREADY out
        field ATVALID: b1 bit[0]  // Integration Mode instruction ATVALID out
    }
    // Claim Tag Set Register
    reg TRCCLAIMSET offset 0x41FA0 {
        field SET3: b1 bit[3]  // When a write to one of these bits occurs, with the value:
        field SET2: b1 bit[2]  // When a write to one of these bits occurs, with the value:
        field SET1: b1 bit[1]  // When a write to one of these bits occurs, with the value:
        field SET0: b1 bit[0]  // When a write to one of these bits occurs, with the value:
    }
    // Claim Tag Clear Register
    reg TRCCLAIMCLR offset 0x41FA4 {
        field CLR3: b1 bit[3]  // When a write to one of these bits occurs, with the value:
        field CLR2: b1 bit[2]  // When a write to one of these bits occurs, with the value:
        field CLR1: b1 bit[1]  // When a write to one of these bits occurs, with the value:
        field CLR0: b1 bit[0]  // When a write to one of these bits occurs, with the value:
    }
    // Returns the level of tracing that the trace unit can support
    reg TRCAUTHSTATUS offset 0x41FB8 {
        field SNID: u32 bit[6..7] readonly  // Indicates whether the system enables the trace unit to support Secure non-invasive debug:
        field SID: u32 bit[4..5] readonly  // Indicates whether the trace unit supports Secure invasive debug:
        field NSNID: u32 bit[2..3] readonly  // Indicates whether the system enables the trace unit to support Non-secure non-invasive debug:
        field NSID: u32 bit[0..1] readonly  // Indicates whether the trace unit supports Non-secure invasive debug:
    }
    // TRCDEVARCH
    reg TRCDEVARCH offset 0x41FBC {
        field ARCHITECT: u32 bit[21..31] readonly  // reads as 0b01000111011
        field PRESENT: b1 bit[20] readonly  // reads as 0b1
        field REVISION: u32 bit[16..19] readonly  // reads as 0b0000
        field ARCHID: u32 bit[0..15] readonly  // reads as 0b0100101000010011
    }
    // TRCDEVID
    reg TRCDEVID offset 0x41FC8 {
        field TRCDEVID: u32 bit[0..31]
    }
    // TRCDEVTYPE
    reg TRCDEVTYPE offset 0x41FCC {
        field SUB: u32 bit[4..7] readonly  // reads as 0b0001
        field MAJOR: u32 bit[0..3] readonly  // reads as 0b0011
    }
    // TRCPIDR4
    reg TRCPIDR4 offset 0x41FD0 {
        field SIZE: u32 bit[4..7] readonly  // reads as `ImpDef
        field DES_2: u32 bit[0..3] readonly  // reads as `ImpDef
    }
    // TRCPIDR5
    reg TRCPIDR5 offset 0x41FD4 {
        field TRCPIDR5: u32 bit[0..31]
    }
    // TRCPIDR6
    reg TRCPIDR6 offset 0x41FD8 {
        field TRCPIDR6: u32 bit[0..31]
    }
    // TRCPIDR7
    reg TRCPIDR7 offset 0x41FDC {
        field TRCPIDR7: u32 bit[0..31]
    }
    // TRCPIDR0
    reg TRCPIDR0 offset 0x41FE0 {
        field PART_0: u32 bit[0..7] readonly  // reads as `ImpDef
    }
    // TRCPIDR1
    reg TRCPIDR1 offset 0x41FE4 {
        field DES_0: u32 bit[4..7] readonly  // reads as `ImpDef
        field PART_0: u32 bit[0..3] readonly  // reads as `ImpDef
    }
    // TRCPIDR2
    reg TRCPIDR2 offset 0x41FE8 {
        field REVISION: u32 bit[4..7] readonly  // reads as `ImpDef
        field JEDEC: b1 bit[3] readonly  // reads as 0b1
        field DES_0: u32 bit[0..2] readonly  // reads as `ImpDef
    }
    // TRCPIDR3
    reg TRCPIDR3 offset 0x41FEC {
        field REVAND: u32 bit[4..7] readonly  // reads as `ImpDef
        field CMOD: u32 bit[0..3] readonly  // reads as `ImpDef
    }
    // TRCCIDR0
    reg TRCCIDR0 offset 0x41FF0 {
        field PRMBL_0: u32 bit[0..7] readonly  // reads as 0b00001101
    }
    // TRCCIDR1
    reg TRCCIDR1 offset 0x41FF4 {
        field CLASS: u32 bit[4..7] readonly  // reads as 0b1001
        field PRMBL_1: u32 bit[0..3] readonly  // reads as 0b0000
    }
    // TRCCIDR2
    reg TRCCIDR2 offset 0x41FF8 {
        field PRMBL_2: u32 bit[0..7] readonly  // reads as 0b00000101
    }
    // TRCCIDR3
    reg TRCCIDR3 offset 0x41FFC {
        field PRMBL_3: u32 bit[0..7] readonly  // reads as 0b10110001
    }
    // CTI Control Register
    reg CTICONTROL offset 0x42000 {
        field GLBEN: b1 bit[0]  // Enables or disables the CTI
    }
    // CTI Interrupt Acknowledge Register
    reg CTIINTACK offset 0x42010 {
        field INTACK: u32 bit[0..7]  // Acknowledges the corresponding ctitrigout output. There is one bit of the register for each ctitrigout output. When a 1 is written to a bit in this register, the corresponding ctitrigout is acknowledged, causing it to be cleared.
    }
    // CTI Application Trigger Set Register
    reg CTIAPPSET offset 0x42014 {
        field APPSET: u32 bit[0..3]  // Setting a bit HIGH generates a channel event for the selected channel. There is one bit of the register for each channel
    }
    // CTI Application Trigger Clear Register
    reg CTIAPPCLEAR offset 0x42018 {
        field APPCLEAR: u32 bit[0..3]  // Sets the corresponding bits in the CTIAPPSET to 0. There is one bit of the register for each channel.
    }
    // CTI Application Pulse Register
    reg CTIAPPPULSE offset 0x4201C {
        field APPULSE: u32 bit[0..3]  // Setting a bit HIGH generates a channel event pulse for the selected channel. There is one bit of the register for each channel.
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIINEN0 offset 0x42020 {
        field TRIGINEN: u32 bit[0..3]  // Enables a cross trigger event to the corresponding channel when a ctitrigin input is activated. There is one bit of the field for each of the four channels
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIINEN1 offset 0x42024 {
        field TRIGINEN: u32 bit[0..3]  // Enables a cross trigger event to the corresponding channel when a ctitrigin input is activated. There is one bit of the field for each of the four channels
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIINEN2 offset 0x42028 {
        field TRIGINEN: u32 bit[0..3]  // Enables a cross trigger event to the corresponding channel when a ctitrigin input is activated. There is one bit of the field for each of the four channels
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIINEN3 offset 0x4202C {
        field TRIGINEN: u32 bit[0..3]  // Enables a cross trigger event to the corresponding channel when a ctitrigin input is activated. There is one bit of the field for each of the four channels
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIINEN4 offset 0x42030 {
        field TRIGINEN: u32 bit[0..3]  // Enables a cross trigger event to the corresponding channel when a ctitrigin input is activated. There is one bit of the field for each of the four channels
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIINEN5 offset 0x42034 {
        field TRIGINEN: u32 bit[0..3]  // Enables a cross trigger event to the corresponding channel when a ctitrigin input is activated. There is one bit of the field for each of the four channels
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIINEN6 offset 0x42038 {
        field TRIGINEN: u32 bit[0..3]  // Enables a cross trigger event to the corresponding channel when a ctitrigin input is activated. There is one bit of the field for each of the four channels
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIINEN7 offset 0x4203C {
        field TRIGINEN: u32 bit[0..3]  // Enables a cross trigger event to the corresponding channel when a ctitrigin input is activated. There is one bit of the field for each of the four channels
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIOUTEN0 offset 0x420A0 {
        field TRIGOUTEN: u32 bit[0..3]  // Enables a cross trigger event to ctitrigout when the corresponding channel is activated. There is one bit of the field for each of the four channels.
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIOUTEN1 offset 0x420A4 {
        field TRIGOUTEN: u32 bit[0..3]  // Enables a cross trigger event to ctitrigout when the corresponding channel is activated. There is one bit of the field for each of the four channels.
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIOUTEN2 offset 0x420A8 {
        field TRIGOUTEN: u32 bit[0..3]  // Enables a cross trigger event to ctitrigout when the corresponding channel is activated. There is one bit of the field for each of the four channels.
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIOUTEN3 offset 0x420AC {
        field TRIGOUTEN: u32 bit[0..3]  // Enables a cross trigger event to ctitrigout when the corresponding channel is activated. There is one bit of the field for each of the four channels.
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIOUTEN4 offset 0x420B0 {
        field TRIGOUTEN: u32 bit[0..3]  // Enables a cross trigger event to ctitrigout when the corresponding channel is activated. There is one bit of the field for each of the four channels.
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIOUTEN5 offset 0x420B4 {
        field TRIGOUTEN: u32 bit[0..3]  // Enables a cross trigger event to ctitrigout when the corresponding channel is activated. There is one bit of the field for each of the four channels.
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIOUTEN6 offset 0x420B8 {
        field TRIGOUTEN: u32 bit[0..3]  // Enables a cross trigger event to ctitrigout when the corresponding channel is activated. There is one bit of the field for each of the four channels.
    }
    // CTI Trigger to Channel Enable Registers
    reg CTIOUTEN7 offset 0x420BC {
        field TRIGOUTEN: u32 bit[0..3]  // Enables a cross trigger event to ctitrigout when the corresponding channel is activated. There is one bit of the field for each of the four channels.
    }
    // CTI Trigger to Channel Enable Registers
    reg CTITRIGINSTATUS offset 0x42130 {
        field TRIGINSTATUS: u32 bit[0..7] readonly  // Shows the status of the ctitrigin inputs. There is one bit of the field for each trigger input.Because the register provides a view of the raw ctitrigin inputs, the reset value is UNKNOWN.
    }
    // CTI Trigger In Status Register
    reg CTITRIGOUTSTATUS offset 0x42134 {
        field TRIGOUTSTATUS: u32 bit[0..7] readonly  // Shows the status of the ctitrigout outputs. There is one bit of the field for each trigger output.
    }
    // CTI Channel In Status Register
    reg CTICHINSTATUS offset 0x42138 {
        field CTICHOUTSTATUS: u32 bit[0..3] readonly  // Shows the status of the ctichout outputs. There is one bit of the field for each channel output
    }
    // Enable CTI Channel Gate register
    reg CTIGATE offset 0x42140 {
        field CTIGATEEN3: b1 bit[3]  // Enable ctichout3. Set to 0 to disable channel propagation.
        field CTIGATEEN2: b1 bit[2]  // Enable ctichout2. Set to 0 to disable channel propagation.
        field CTIGATEEN1: b1 bit[1]  // Enable ctichout1. Set to 0 to disable channel propagation.
        field CTIGATEEN0: b1 bit[0]  // Enable ctichout0. Set to 0 to disable channel propagation.
    }
    // External Multiplexer Control register
    reg ASICCTL offset 0x42144 {
        field ASICCTL: u32 bit[0..31]
    }
    // Integration Test Channel Output register
    reg ITCHOUT offset 0x42EE4 {
        field CTCHOUT: u32 bit[0..3]  // Sets the value of the ctichout outputs
    }
    // Integration Test Trigger Output register
    reg ITTRIGOUT offset 0x42EE8 {
        field CTTRIGOUT: u32 bit[0..7]  // Sets the value of the ctitrigout outputs
    }
    // Integration Test Channel Input register
    reg ITCHIN offset 0x42EF4 {
        field CTCHIN: u32 bit[0..3] readonly  // Reads the value of the ctichin inputs.
    }
    // Integration Mode Control register
    reg ITCTRL offset 0x42F00 {
        field IME: b1 bit[0]  // Integration Mode Enable
    }
    // Device Architecture register
    reg DEVARCH offset 0x42FBC {
        field ARCHITECT: u32 bit[21..31] readonly  // Indicates the component architect
        field PRESENT: b1 bit[20] readonly  // Indicates whether the DEVARCH register is present
        field REVISION: u32 bit[16..19] readonly  // Indicates the architecture revision
        field ARCHID: u32 bit[0..15] readonly  // Indicates the component
    }
    // Device Configuration register
    reg DEVID offset 0x42FC8 {
        field NUMCH: u32 bit[16..19] readonly  // Number of ECT channels available
        field NUMTRIG: u32 bit[8..15] readonly  // Number of ECT triggers available.
        field EXTMUXNUM: u32 bit[0..4] readonly  // Indicates the number of multiplexers available on Trigger Inputs and Trigger Outputs that are using asicctl. The default value of 0b00000 indicates that no multiplexing is present. This value of this bit depends on the Verilog define EXTMUXNUM that you must change accordingly.
    }
    // Device Type Identifier register
    reg DEVTYPE offset 0x42FCC {
        field SUB: u32 bit[4..7] readonly  // Sub-classification of the type of the debug component as specified in the ARM Architecture Specification within the major classification as specified in the MAJOR field.
        field MAJOR: u32 bit[0..3] readonly  // Major classification of the type of the debug component as specified in the ARM Architecture Specification for this debug and trace component.
    }
    // CoreSight Peripheral ID4
    reg PIDR4 offset 0x42FD0 {
        field SIZE: u32 bit[4..7] readonly  // Always 0b0000. Indicates that the device only occupies 4KB of memory
        field DES_2: u32 bit[0..3] readonly  // Together, PIDR1.DES_0, PIDR2.DES_1, and PIDR4.DES_2 identify the designer of the component.
    }
    // CoreSight Peripheral ID5
    reg PIDR5 offset 0x42FD4 {
        field PIDR5: u32 bit[0..31]
    }
    // CoreSight Peripheral ID6
    reg PIDR6 offset 0x42FD8 {
        field PIDR6: u32 bit[0..31]
    }
    // CoreSight Peripheral ID7
    reg PIDR7 offset 0x42FDC {
        field PIDR7: u32 bit[0..31]
    }
    // CoreSight Peripheral ID0
    reg PIDR0 offset 0x42FE0 {
        field PART_0: u32 bit[0..7] readonly  // Bits[7:0] of the 12-bit part number of the component. The designer of the component assigns this part number.
    }
    // CoreSight Peripheral ID1
    reg PIDR1 offset 0x42FE4 {
        field DES_0: u32 bit[4..7] readonly  // Together, PIDR1.DES_0, PIDR2.DES_1, and PIDR4.DES_2 identify the designer of the component.
        field PART_1: u32 bit[0..3] readonly  // Bits[11:8] of the 12-bit part number of the component. The designer of the component assigns this part number.
    }
    // CoreSight Peripheral ID2
    reg PIDR2 offset 0x42FE8 {
        field REVISION: u32 bit[4..7] readonly  // This device is at r1p0
        field JEDEC: b1 bit[3] readonly  // Always 1. Indicates that the JEDEC-assigned designer ID is used.
        field DES_1: u32 bit[0..2] readonly  // Together, PIDR1.DES_0, PIDR2.DES_1, and PIDR4.DES_2 identify the designer of the component.
    }
    // CoreSight Peripheral ID3
    reg PIDR3 offset 0x42FEC {
        field REVAND: u32 bit[4..7] readonly  // Indicates minor errata fixes specific to the revision of the component being used, for example metal fixes after implementation. In most cases, this field is 0b0000. ARM recommends that the component designers ensure that a metal fix can change this field if required, for example, by driving it from registers that reset to 0b0000.
        field CMOD: u32 bit[0..3] readonly  // Customer Modified. Indicates whether the customer has modified the behavior of the component. In most cases, this field is 0b0000. Customers change this value when they make authorized modifications to this component.
    }
    // CoreSight Component ID0
    reg CIDR0 offset 0x42FF0 {
        field PRMBL_0: u32 bit[0..7] readonly  // Preamble[0]. Contains bits[7:0] of the component identification code
    }
    // CoreSight Component ID1
    reg CIDR1 offset 0x42FF4 {
        field CLASS: u32 bit[4..7] readonly  // Class of the component, for example, whether the component is a ROM table or a generic CoreSight component. Contains bits[15:12] of the component identification code.
        field PRMBL_1: u32 bit[0..3] readonly  // Preamble[1]. Contains bits[11:8] of the component identification code.
    }
    // CoreSight Component ID2
    reg CIDR2 offset 0x42FF8 {
        field PRMBL_2: u32 bit[0..7] readonly  // Preamble[2]. Contains bits[23:16] of the component identification code.
    }
    // CoreSight Component ID3
    reg CIDR3 offset 0x42FFC {
        field PRMBL_3: u32 bit[0..7] readonly  // Preamble[3]. Contains bits[31:24] of the component identification code.
    }
}

