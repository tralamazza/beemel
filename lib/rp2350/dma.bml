// Generated by bml-svd
// Vendor: Raspberry Pi
// Device: RP2350 (0.1)
// 
//         Dual Cortex-M33 or Hazard3 processors at 150MHz
//         520kB on-chip SRAM, in 10 independent banks
//         Extended low-power sleep states with optional SRAM retention: as low as 10uA DVDD
//         8kB of one-time-programmable storage (OTP)
//         Up to 16MB of external QSPI flash/PSRAM via dedicated QSPI bus
//             Additional 16MB flash/PSRAM accessible via optional second chip-select
//         On-chip switched-mode power supply to generate core voltage
//             Low-quiescent-current LDO mode can be enabled for sleep states
//         2x on-chip PLLs for internal or external clock generation
//         GPIOs are 5V-tolerant (powered), and 3.3V-failsafe (unpowered)
//         Security features:
//             Optional boot signing, enforced by on-chip mask ROM, with key fingerprint in OTP
//             Protected OTP storage for optional boot decryption key
//             Global bus filtering based on Arm or RISC-V security/privilege levels
//             Peripherals, GPIOs and DMA channels individually assignable to security domains
//             Hardware mitigations for fault injection attacks
//             Hardware SHA-256 accelerator
//         Peripherals:
//             2x UARTs
//             2x SPI controllers
//             2x I2C controllers
//             24x PWM channels
//             USB 1.1 controller and PHY, with host and device support
//             12x PIO state machines
//             1x HSTX peripheral
//     

export enum CH0_TRANS_COUNT_MODE: u8 {
    NORMAL = 0,
    TRIGGER_SELF = 1,
    ENDLESS = 15,
}
export enum CH0_CTRL_TRIG_TREQ_SEL: u8 {
    PIO0_TX0 = 0,
    PIO0_TX1 = 1,
    PIO0_TX2 = 2,
    PIO0_TX3 = 3,
    PIO0_RX0 = 4,
    PIO0_RX1 = 5,
    PIO0_RX2 = 6,
    PIO0_RX3 = 7,
    PIO1_TX0 = 8,
    PIO1_TX1 = 9,
    PIO1_TX2 = 10,
    PIO1_TX3 = 11,
    PIO1_RX0 = 12,
    PIO1_RX1 = 13,
    PIO1_RX2 = 14,
    PIO1_RX3 = 15,
    PIO2_TX0 = 16,
    PIO2_TX1 = 17,
    PIO2_TX2 = 18,
    PIO2_TX3 = 19,
    PIO2_RX0 = 20,
    PIO2_RX1 = 21,
    PIO2_RX2 = 22,
    PIO2_RX3 = 23,
    SPI0_TX = 24,
    SPI0_RX = 25,
    SPI1_TX = 26,
    SPI1_RX = 27,
    UART0_TX = 28,
    UART0_RX = 29,
    UART1_TX = 30,
    UART1_RX = 31,
    PWM_WRAP0 = 32,
    PWM_WRAP1 = 33,
    PWM_WRAP2 = 34,
    PWM_WRAP3 = 35,
    PWM_WRAP4 = 36,
    PWM_WRAP5 = 37,
    PWM_WRAP6 = 38,
    PWM_WRAP7 = 39,
    PWM_WRAP8 = 40,
    PWM_WRAP9 = 41,
    PWM_WRAP10 = 42,
    PWM_WRAP11 = 43,
    I2C0_TX = 44,
    I2C0_RX = 45,
    I2C1_TX = 46,
    I2C1_RX = 47,
    ADC = 48,
    XIP_STREAM = 49,
    XIP_QMITX = 50,
    XIP_QMIRX = 51,
    HSTX = 52,
    CORESIGHT = 53,
    SHA256 = 54,
    TIMER0 = 59,
    TIMER1 = 60,
    TIMER2 = 61,
    TIMER3 = 62,
    PERMANENT = 63,
}
export enum CH0_CTRL_TRIG_RING_SIZE: u8 {
    RING_NONE = 0,
}
export enum CH0_CTRL_TRIG_DATA_SIZE: u8 {
    SIZE_BYTE = 0,
    SIZE_HALFWORD = 1,
    SIZE_WORD = 2,
}
export enum SNIFF_CTRL_CALC: u8 {
    CRC32 = 0,
    CRC32R = 1,
    CRC16 = 2,
    CRC16R = 3,
    EVEN = 14,
    SUM = 15,
}

// DMA with separate read and write masters
peripheral DMA at 0x50000000 {
    // DMA Channel 0 Read Address pointer
    reg CH0_READ_ADDR offset 0x00 {
        field CH0_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 0 Write Address pointer
    reg CH0_WRITE_ADDR offset 0x04 {
        field CH0_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 0 Transfer Count
    reg CH0_TRANS_COUNT offset 0x08 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 0 Control and Status
    reg CH0_CTRL_TRIG offset 0x0C {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 0 CTRL register
    reg CH0_AL1_CTRL offset 0x10 {
        field CH0_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 0 READ_ADDR register
    reg CH0_AL1_READ_ADDR offset 0x14 {
        field CH0_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 0 WRITE_ADDR register
    reg CH0_AL1_WRITE_ADDR offset 0x18 {
        field CH0_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 0 TRANS_COUNT register 
    reg CH0_AL1_TRANS_COUNT_TRIG offset 0x1C {
        field CH0_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 0 CTRL register
    reg CH0_AL2_CTRL offset 0x20 {
        field CH0_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 0 TRANS_COUNT register
    reg CH0_AL2_TRANS_COUNT offset 0x24 {
        field CH0_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 0 READ_ADDR register
    reg CH0_AL2_READ_ADDR offset 0x28 {
        field CH0_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 0 WRITE_ADDR register 
    reg CH0_AL2_WRITE_ADDR_TRIG offset 0x2C {
        field CH0_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 0 CTRL register
    reg CH0_AL3_CTRL offset 0x30 {
        field CH0_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 0 WRITE_ADDR register
    reg CH0_AL3_WRITE_ADDR offset 0x34 {
        field CH0_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 0 TRANS_COUNT register
    reg CH0_AL3_TRANS_COUNT offset 0x38 {
        field CH0_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 0 READ_ADDR register 
    reg CH0_AL3_READ_ADDR_TRIG offset 0x3C {
        field CH0_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 1 Read Address pointer
    reg CH1_READ_ADDR offset 0x40 {
        field CH1_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 1 Write Address pointer
    reg CH1_WRITE_ADDR offset 0x44 {
        field CH1_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 1 Transfer Count
    reg CH1_TRANS_COUNT offset 0x48 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 1 Control and Status
    reg CH1_CTRL_TRIG offset 0x4C {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 1 CTRL register
    reg CH1_AL1_CTRL offset 0x50 {
        field CH1_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 1 READ_ADDR register
    reg CH1_AL1_READ_ADDR offset 0x54 {
        field CH1_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 1 WRITE_ADDR register
    reg CH1_AL1_WRITE_ADDR offset 0x58 {
        field CH1_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 1 TRANS_COUNT register 
    reg CH1_AL1_TRANS_COUNT_TRIG offset 0x5C {
        field CH1_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 1 CTRL register
    reg CH1_AL2_CTRL offset 0x60 {
        field CH1_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 1 TRANS_COUNT register
    reg CH1_AL2_TRANS_COUNT offset 0x64 {
        field CH1_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 1 READ_ADDR register
    reg CH1_AL2_READ_ADDR offset 0x68 {
        field CH1_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 1 WRITE_ADDR register 
    reg CH1_AL2_WRITE_ADDR_TRIG offset 0x6C {
        field CH1_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 1 CTRL register
    reg CH1_AL3_CTRL offset 0x70 {
        field CH1_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 1 WRITE_ADDR register
    reg CH1_AL3_WRITE_ADDR offset 0x74 {
        field CH1_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 1 TRANS_COUNT register
    reg CH1_AL3_TRANS_COUNT offset 0x78 {
        field CH1_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 1 READ_ADDR register 
    reg CH1_AL3_READ_ADDR_TRIG offset 0x7C {
        field CH1_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 2 Read Address pointer
    reg CH2_READ_ADDR offset 0x80 {
        field CH2_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 2 Write Address pointer
    reg CH2_WRITE_ADDR offset 0x84 {
        field CH2_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 2 Transfer Count
    reg CH2_TRANS_COUNT offset 0x88 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 2 Control and Status
    reg CH2_CTRL_TRIG offset 0x8C {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 2 CTRL register
    reg CH2_AL1_CTRL offset 0x90 {
        field CH2_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 2 READ_ADDR register
    reg CH2_AL1_READ_ADDR offset 0x94 {
        field CH2_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 2 WRITE_ADDR register
    reg CH2_AL1_WRITE_ADDR offset 0x98 {
        field CH2_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 2 TRANS_COUNT register 
    reg CH2_AL1_TRANS_COUNT_TRIG offset 0x9C {
        field CH2_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 2 CTRL register
    reg CH2_AL2_CTRL offset 0xA0 {
        field CH2_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 2 TRANS_COUNT register
    reg CH2_AL2_TRANS_COUNT offset 0xA4 {
        field CH2_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 2 READ_ADDR register
    reg CH2_AL2_READ_ADDR offset 0xA8 {
        field CH2_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 2 WRITE_ADDR register 
    reg CH2_AL2_WRITE_ADDR_TRIG offset 0xAC {
        field CH2_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 2 CTRL register
    reg CH2_AL3_CTRL offset 0xB0 {
        field CH2_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 2 WRITE_ADDR register
    reg CH2_AL3_WRITE_ADDR offset 0xB4 {
        field CH2_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 2 TRANS_COUNT register
    reg CH2_AL3_TRANS_COUNT offset 0xB8 {
        field CH2_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 2 READ_ADDR register 
    reg CH2_AL3_READ_ADDR_TRIG offset 0xBC {
        field CH2_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 3 Read Address pointer
    reg CH3_READ_ADDR offset 0xC0 {
        field CH3_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 3 Write Address pointer
    reg CH3_WRITE_ADDR offset 0xC4 {
        field CH3_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 3 Transfer Count
    reg CH3_TRANS_COUNT offset 0xC8 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 3 Control and Status
    reg CH3_CTRL_TRIG offset 0xCC {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 3 CTRL register
    reg CH3_AL1_CTRL offset 0xD0 {
        field CH3_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 3 READ_ADDR register
    reg CH3_AL1_READ_ADDR offset 0xD4 {
        field CH3_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 3 WRITE_ADDR register
    reg CH3_AL1_WRITE_ADDR offset 0xD8 {
        field CH3_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 3 TRANS_COUNT register 
    reg CH3_AL1_TRANS_COUNT_TRIG offset 0xDC {
        field CH3_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 3 CTRL register
    reg CH3_AL2_CTRL offset 0xE0 {
        field CH3_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 3 TRANS_COUNT register
    reg CH3_AL2_TRANS_COUNT offset 0xE4 {
        field CH3_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 3 READ_ADDR register
    reg CH3_AL2_READ_ADDR offset 0xE8 {
        field CH3_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 3 WRITE_ADDR register 
    reg CH3_AL2_WRITE_ADDR_TRIG offset 0xEC {
        field CH3_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 3 CTRL register
    reg CH3_AL3_CTRL offset 0xF0 {
        field CH3_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 3 WRITE_ADDR register
    reg CH3_AL3_WRITE_ADDR offset 0xF4 {
        field CH3_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 3 TRANS_COUNT register
    reg CH3_AL3_TRANS_COUNT offset 0xF8 {
        field CH3_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 3 READ_ADDR register 
    reg CH3_AL3_READ_ADDR_TRIG offset 0xFC {
        field CH3_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 4 Read Address pointer
    reg CH4_READ_ADDR offset 0x100 {
        field CH4_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 4 Write Address pointer
    reg CH4_WRITE_ADDR offset 0x104 {
        field CH4_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 4 Transfer Count
    reg CH4_TRANS_COUNT offset 0x108 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 4 Control and Status
    reg CH4_CTRL_TRIG offset 0x10C {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 4 CTRL register
    reg CH4_AL1_CTRL offset 0x110 {
        field CH4_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 4 READ_ADDR register
    reg CH4_AL1_READ_ADDR offset 0x114 {
        field CH4_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 4 WRITE_ADDR register
    reg CH4_AL1_WRITE_ADDR offset 0x118 {
        field CH4_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 4 TRANS_COUNT register 
    reg CH4_AL1_TRANS_COUNT_TRIG offset 0x11C {
        field CH4_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 4 CTRL register
    reg CH4_AL2_CTRL offset 0x120 {
        field CH4_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 4 TRANS_COUNT register
    reg CH4_AL2_TRANS_COUNT offset 0x124 {
        field CH4_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 4 READ_ADDR register
    reg CH4_AL2_READ_ADDR offset 0x128 {
        field CH4_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 4 WRITE_ADDR register 
    reg CH4_AL2_WRITE_ADDR_TRIG offset 0x12C {
        field CH4_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 4 CTRL register
    reg CH4_AL3_CTRL offset 0x130 {
        field CH4_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 4 WRITE_ADDR register
    reg CH4_AL3_WRITE_ADDR offset 0x134 {
        field CH4_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 4 TRANS_COUNT register
    reg CH4_AL3_TRANS_COUNT offset 0x138 {
        field CH4_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 4 READ_ADDR register 
    reg CH4_AL3_READ_ADDR_TRIG offset 0x13C {
        field CH4_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 5 Read Address pointer
    reg CH5_READ_ADDR offset 0x140 {
        field CH5_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 5 Write Address pointer
    reg CH5_WRITE_ADDR offset 0x144 {
        field CH5_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 5 Transfer Count
    reg CH5_TRANS_COUNT offset 0x148 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 5 Control and Status
    reg CH5_CTRL_TRIG offset 0x14C {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 5 CTRL register
    reg CH5_AL1_CTRL offset 0x150 {
        field CH5_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 5 READ_ADDR register
    reg CH5_AL1_READ_ADDR offset 0x154 {
        field CH5_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 5 WRITE_ADDR register
    reg CH5_AL1_WRITE_ADDR offset 0x158 {
        field CH5_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 5 TRANS_COUNT register 
    reg CH5_AL1_TRANS_COUNT_TRIG offset 0x15C {
        field CH5_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 5 CTRL register
    reg CH5_AL2_CTRL offset 0x160 {
        field CH5_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 5 TRANS_COUNT register
    reg CH5_AL2_TRANS_COUNT offset 0x164 {
        field CH5_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 5 READ_ADDR register
    reg CH5_AL2_READ_ADDR offset 0x168 {
        field CH5_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 5 WRITE_ADDR register 
    reg CH5_AL2_WRITE_ADDR_TRIG offset 0x16C {
        field CH5_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 5 CTRL register
    reg CH5_AL3_CTRL offset 0x170 {
        field CH5_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 5 WRITE_ADDR register
    reg CH5_AL3_WRITE_ADDR offset 0x174 {
        field CH5_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 5 TRANS_COUNT register
    reg CH5_AL3_TRANS_COUNT offset 0x178 {
        field CH5_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 5 READ_ADDR register 
    reg CH5_AL3_READ_ADDR_TRIG offset 0x17C {
        field CH5_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 6 Read Address pointer
    reg CH6_READ_ADDR offset 0x180 {
        field CH6_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 6 Write Address pointer
    reg CH6_WRITE_ADDR offset 0x184 {
        field CH6_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 6 Transfer Count
    reg CH6_TRANS_COUNT offset 0x188 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 6 Control and Status
    reg CH6_CTRL_TRIG offset 0x18C {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 6 CTRL register
    reg CH6_AL1_CTRL offset 0x190 {
        field CH6_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 6 READ_ADDR register
    reg CH6_AL1_READ_ADDR offset 0x194 {
        field CH6_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 6 WRITE_ADDR register
    reg CH6_AL1_WRITE_ADDR offset 0x198 {
        field CH6_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 6 TRANS_COUNT register 
    reg CH6_AL1_TRANS_COUNT_TRIG offset 0x19C {
        field CH6_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 6 CTRL register
    reg CH6_AL2_CTRL offset 0x1A0 {
        field CH6_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 6 TRANS_COUNT register
    reg CH6_AL2_TRANS_COUNT offset 0x1A4 {
        field CH6_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 6 READ_ADDR register
    reg CH6_AL2_READ_ADDR offset 0x1A8 {
        field CH6_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 6 WRITE_ADDR register 
    reg CH6_AL2_WRITE_ADDR_TRIG offset 0x1AC {
        field CH6_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 6 CTRL register
    reg CH6_AL3_CTRL offset 0x1B0 {
        field CH6_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 6 WRITE_ADDR register
    reg CH6_AL3_WRITE_ADDR offset 0x1B4 {
        field CH6_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 6 TRANS_COUNT register
    reg CH6_AL3_TRANS_COUNT offset 0x1B8 {
        field CH6_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 6 READ_ADDR register 
    reg CH6_AL3_READ_ADDR_TRIG offset 0x1BC {
        field CH6_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 7 Read Address pointer
    reg CH7_READ_ADDR offset 0x1C0 {
        field CH7_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 7 Write Address pointer
    reg CH7_WRITE_ADDR offset 0x1C4 {
        field CH7_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 7 Transfer Count
    reg CH7_TRANS_COUNT offset 0x1C8 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 7 Control and Status
    reg CH7_CTRL_TRIG offset 0x1CC {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 7 CTRL register
    reg CH7_AL1_CTRL offset 0x1D0 {
        field CH7_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 7 READ_ADDR register
    reg CH7_AL1_READ_ADDR offset 0x1D4 {
        field CH7_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 7 WRITE_ADDR register
    reg CH7_AL1_WRITE_ADDR offset 0x1D8 {
        field CH7_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 7 TRANS_COUNT register 
    reg CH7_AL1_TRANS_COUNT_TRIG offset 0x1DC {
        field CH7_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 7 CTRL register
    reg CH7_AL2_CTRL offset 0x1E0 {
        field CH7_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 7 TRANS_COUNT register
    reg CH7_AL2_TRANS_COUNT offset 0x1E4 {
        field CH7_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 7 READ_ADDR register
    reg CH7_AL2_READ_ADDR offset 0x1E8 {
        field CH7_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 7 WRITE_ADDR register 
    reg CH7_AL2_WRITE_ADDR_TRIG offset 0x1EC {
        field CH7_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 7 CTRL register
    reg CH7_AL3_CTRL offset 0x1F0 {
        field CH7_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 7 WRITE_ADDR register
    reg CH7_AL3_WRITE_ADDR offset 0x1F4 {
        field CH7_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 7 TRANS_COUNT register
    reg CH7_AL3_TRANS_COUNT offset 0x1F8 {
        field CH7_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 7 READ_ADDR register 
    reg CH7_AL3_READ_ADDR_TRIG offset 0x1FC {
        field CH7_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 8 Read Address pointer
    reg CH8_READ_ADDR offset 0x200 {
        field CH8_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 8 Write Address pointer
    reg CH8_WRITE_ADDR offset 0x204 {
        field CH8_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 8 Transfer Count
    reg CH8_TRANS_COUNT offset 0x208 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 8 Control and Status
    reg CH8_CTRL_TRIG offset 0x20C {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 8 CTRL register
    reg CH8_AL1_CTRL offset 0x210 {
        field CH8_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 8 READ_ADDR register
    reg CH8_AL1_READ_ADDR offset 0x214 {
        field CH8_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 8 WRITE_ADDR register
    reg CH8_AL1_WRITE_ADDR offset 0x218 {
        field CH8_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 8 TRANS_COUNT register 
    reg CH8_AL1_TRANS_COUNT_TRIG offset 0x21C {
        field CH8_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 8 CTRL register
    reg CH8_AL2_CTRL offset 0x220 {
        field CH8_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 8 TRANS_COUNT register
    reg CH8_AL2_TRANS_COUNT offset 0x224 {
        field CH8_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 8 READ_ADDR register
    reg CH8_AL2_READ_ADDR offset 0x228 {
        field CH8_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 8 WRITE_ADDR register 
    reg CH8_AL2_WRITE_ADDR_TRIG offset 0x22C {
        field CH8_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 8 CTRL register
    reg CH8_AL3_CTRL offset 0x230 {
        field CH8_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 8 WRITE_ADDR register
    reg CH8_AL3_WRITE_ADDR offset 0x234 {
        field CH8_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 8 TRANS_COUNT register
    reg CH8_AL3_TRANS_COUNT offset 0x238 {
        field CH8_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 8 READ_ADDR register 
    reg CH8_AL3_READ_ADDR_TRIG offset 0x23C {
        field CH8_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 9 Read Address pointer
    reg CH9_READ_ADDR offset 0x240 {
        field CH9_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 9 Write Address pointer
    reg CH9_WRITE_ADDR offset 0x244 {
        field CH9_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 9 Transfer Count
    reg CH9_TRANS_COUNT offset 0x248 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 9 Control and Status
    reg CH9_CTRL_TRIG offset 0x24C {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 9 CTRL register
    reg CH9_AL1_CTRL offset 0x250 {
        field CH9_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 9 READ_ADDR register
    reg CH9_AL1_READ_ADDR offset 0x254 {
        field CH9_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 9 WRITE_ADDR register
    reg CH9_AL1_WRITE_ADDR offset 0x258 {
        field CH9_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 9 TRANS_COUNT register 
    reg CH9_AL1_TRANS_COUNT_TRIG offset 0x25C {
        field CH9_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 9 CTRL register
    reg CH9_AL2_CTRL offset 0x260 {
        field CH9_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 9 TRANS_COUNT register
    reg CH9_AL2_TRANS_COUNT offset 0x264 {
        field CH9_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 9 READ_ADDR register
    reg CH9_AL2_READ_ADDR offset 0x268 {
        field CH9_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 9 WRITE_ADDR register 
    reg CH9_AL2_WRITE_ADDR_TRIG offset 0x26C {
        field CH9_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 9 CTRL register
    reg CH9_AL3_CTRL offset 0x270 {
        field CH9_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 9 WRITE_ADDR register
    reg CH9_AL3_WRITE_ADDR offset 0x274 {
        field CH9_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 9 TRANS_COUNT register
    reg CH9_AL3_TRANS_COUNT offset 0x278 {
        field CH9_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 9 READ_ADDR register 
    reg CH9_AL3_READ_ADDR_TRIG offset 0x27C {
        field CH9_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 10 Read Address pointer
    reg CH10_READ_ADDR offset 0x280 {
        field CH10_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 10 Write Address pointer
    reg CH10_WRITE_ADDR offset 0x284 {
        field CH10_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 10 Transfer Count
    reg CH10_TRANS_COUNT offset 0x288 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 10 Control and Status
    reg CH10_CTRL_TRIG offset 0x28C {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 10 CTRL register
    reg CH10_AL1_CTRL offset 0x290 {
        field CH10_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 10 READ_ADDR register
    reg CH10_AL1_READ_ADDR offset 0x294 {
        field CH10_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 10 WRITE_ADDR register
    reg CH10_AL1_WRITE_ADDR offset 0x298 {
        field CH10_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 10 TRANS_COUNT register 
    reg CH10_AL1_TRANS_COUNT_TRIG offset 0x29C {
        field CH10_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 10 CTRL register
    reg CH10_AL2_CTRL offset 0x2A0 {
        field CH10_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 10 TRANS_COUNT register
    reg CH10_AL2_TRANS_COUNT offset 0x2A4 {
        field CH10_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 10 READ_ADDR register
    reg CH10_AL2_READ_ADDR offset 0x2A8 {
        field CH10_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 10 WRITE_ADDR register 
    reg CH10_AL2_WRITE_ADDR_TRIG offset 0x2AC {
        field CH10_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 10 CTRL register
    reg CH10_AL3_CTRL offset 0x2B0 {
        field CH10_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 10 WRITE_ADDR register
    reg CH10_AL3_WRITE_ADDR offset 0x2B4 {
        field CH10_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 10 TRANS_COUNT register
    reg CH10_AL3_TRANS_COUNT offset 0x2B8 {
        field CH10_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 10 READ_ADDR register 
    reg CH10_AL3_READ_ADDR_TRIG offset 0x2BC {
        field CH10_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 11 Read Address pointer
    reg CH11_READ_ADDR offset 0x2C0 {
        field CH11_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 11 Write Address pointer
    reg CH11_WRITE_ADDR offset 0x2C4 {
        field CH11_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 11 Transfer Count
    reg CH11_TRANS_COUNT offset 0x2C8 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 11 Control and Status
    reg CH11_CTRL_TRIG offset 0x2CC {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 11 CTRL register
    reg CH11_AL1_CTRL offset 0x2D0 {
        field CH11_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 11 READ_ADDR register
    reg CH11_AL1_READ_ADDR offset 0x2D4 {
        field CH11_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 11 WRITE_ADDR register
    reg CH11_AL1_WRITE_ADDR offset 0x2D8 {
        field CH11_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 11 TRANS_COUNT register 
    reg CH11_AL1_TRANS_COUNT_TRIG offset 0x2DC {
        field CH11_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 11 CTRL register
    reg CH11_AL2_CTRL offset 0x2E0 {
        field CH11_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 11 TRANS_COUNT register
    reg CH11_AL2_TRANS_COUNT offset 0x2E4 {
        field CH11_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 11 READ_ADDR register
    reg CH11_AL2_READ_ADDR offset 0x2E8 {
        field CH11_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 11 WRITE_ADDR register 
    reg CH11_AL2_WRITE_ADDR_TRIG offset 0x2EC {
        field CH11_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 11 CTRL register
    reg CH11_AL3_CTRL offset 0x2F0 {
        field CH11_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 11 WRITE_ADDR register
    reg CH11_AL3_WRITE_ADDR offset 0x2F4 {
        field CH11_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 11 TRANS_COUNT register
    reg CH11_AL3_TRANS_COUNT offset 0x2F8 {
        field CH11_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 11 READ_ADDR register 
    reg CH11_AL3_READ_ADDR_TRIG offset 0x2FC {
        field CH11_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 12 Read Address pointer
    reg CH12_READ_ADDR offset 0x300 {
        field CH12_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 12 Write Address pointer
    reg CH12_WRITE_ADDR offset 0x304 {
        field CH12_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 12 Transfer Count
    reg CH12_TRANS_COUNT offset 0x308 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 12 Control and Status
    reg CH12_CTRL_TRIG offset 0x30C {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 12 CTRL register
    reg CH12_AL1_CTRL offset 0x310 {
        field CH12_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 12 READ_ADDR register
    reg CH12_AL1_READ_ADDR offset 0x314 {
        field CH12_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 12 WRITE_ADDR register
    reg CH12_AL1_WRITE_ADDR offset 0x318 {
        field CH12_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 12 TRANS_COUNT register 
    reg CH12_AL1_TRANS_COUNT_TRIG offset 0x31C {
        field CH12_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 12 CTRL register
    reg CH12_AL2_CTRL offset 0x320 {
        field CH12_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 12 TRANS_COUNT register
    reg CH12_AL2_TRANS_COUNT offset 0x324 {
        field CH12_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 12 READ_ADDR register
    reg CH12_AL2_READ_ADDR offset 0x328 {
        field CH12_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 12 WRITE_ADDR register 
    reg CH12_AL2_WRITE_ADDR_TRIG offset 0x32C {
        field CH12_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 12 CTRL register
    reg CH12_AL3_CTRL offset 0x330 {
        field CH12_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 12 WRITE_ADDR register
    reg CH12_AL3_WRITE_ADDR offset 0x334 {
        field CH12_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 12 TRANS_COUNT register
    reg CH12_AL3_TRANS_COUNT offset 0x338 {
        field CH12_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 12 READ_ADDR register 
    reg CH12_AL3_READ_ADDR_TRIG offset 0x33C {
        field CH12_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 13 Read Address pointer
    reg CH13_READ_ADDR offset 0x340 {
        field CH13_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 13 Write Address pointer
    reg CH13_WRITE_ADDR offset 0x344 {
        field CH13_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 13 Transfer Count
    reg CH13_TRANS_COUNT offset 0x348 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 13 Control and Status
    reg CH13_CTRL_TRIG offset 0x34C {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 13 CTRL register
    reg CH13_AL1_CTRL offset 0x350 {
        field CH13_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 13 READ_ADDR register
    reg CH13_AL1_READ_ADDR offset 0x354 {
        field CH13_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 13 WRITE_ADDR register
    reg CH13_AL1_WRITE_ADDR offset 0x358 {
        field CH13_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 13 TRANS_COUNT register 
    reg CH13_AL1_TRANS_COUNT_TRIG offset 0x35C {
        field CH13_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 13 CTRL register
    reg CH13_AL2_CTRL offset 0x360 {
        field CH13_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 13 TRANS_COUNT register
    reg CH13_AL2_TRANS_COUNT offset 0x364 {
        field CH13_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 13 READ_ADDR register
    reg CH13_AL2_READ_ADDR offset 0x368 {
        field CH13_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 13 WRITE_ADDR register 
    reg CH13_AL2_WRITE_ADDR_TRIG offset 0x36C {
        field CH13_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 13 CTRL register
    reg CH13_AL3_CTRL offset 0x370 {
        field CH13_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 13 WRITE_ADDR register
    reg CH13_AL3_WRITE_ADDR offset 0x374 {
        field CH13_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 13 TRANS_COUNT register
    reg CH13_AL3_TRANS_COUNT offset 0x378 {
        field CH13_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 13 READ_ADDR register 
    reg CH13_AL3_READ_ADDR_TRIG offset 0x37C {
        field CH13_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 14 Read Address pointer
    reg CH14_READ_ADDR offset 0x380 {
        field CH14_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 14 Write Address pointer
    reg CH14_WRITE_ADDR offset 0x384 {
        field CH14_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 14 Transfer Count
    reg CH14_TRANS_COUNT offset 0x388 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 14 Control and Status
    reg CH14_CTRL_TRIG offset 0x38C {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 14 CTRL register
    reg CH14_AL1_CTRL offset 0x390 {
        field CH14_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 14 READ_ADDR register
    reg CH14_AL1_READ_ADDR offset 0x394 {
        field CH14_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 14 WRITE_ADDR register
    reg CH14_AL1_WRITE_ADDR offset 0x398 {
        field CH14_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 14 TRANS_COUNT register 
    reg CH14_AL1_TRANS_COUNT_TRIG offset 0x39C {
        field CH14_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 14 CTRL register
    reg CH14_AL2_CTRL offset 0x3A0 {
        field CH14_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 14 TRANS_COUNT register
    reg CH14_AL2_TRANS_COUNT offset 0x3A4 {
        field CH14_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 14 READ_ADDR register
    reg CH14_AL2_READ_ADDR offset 0x3A8 {
        field CH14_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 14 WRITE_ADDR register 
    reg CH14_AL2_WRITE_ADDR_TRIG offset 0x3AC {
        field CH14_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 14 CTRL register
    reg CH14_AL3_CTRL offset 0x3B0 {
        field CH14_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 14 WRITE_ADDR register
    reg CH14_AL3_WRITE_ADDR offset 0x3B4 {
        field CH14_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 14 TRANS_COUNT register
    reg CH14_AL3_TRANS_COUNT offset 0x3B8 {
        field CH14_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 14 READ_ADDR register 
    reg CH14_AL3_READ_ADDR_TRIG offset 0x3BC {
        field CH14_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // DMA Channel 15 Read Address pointer
    reg CH15_READ_ADDR offset 0x3C0 {
        field CH15_READ_ADDR: u32 bit[0..31]  // This register updates automatically each time a read completes. The current value is the next address to be read by this channel.
    }
    // DMA Channel 15 Write Address pointer
    reg CH15_WRITE_ADDR offset 0x3C4 {
        field CH15_WRITE_ADDR: u32 bit[0..31]  // This register updates automatically each time a write completes. The current value is the next address to be written by this channel.
    }
    // DMA Channel 15 Transfer Count
    reg CH15_TRANS_COUNT offset 0x3C8 {
        field MODE: CH0_TRANS_COUNT_MODE bit[28..31]  // When MODE is 0x0, the transfer count decrements with each transfer until 0, and then the channel triggers the next channel indicated by CTRL_CHAIN_TO.
        field COUNT: u32 bit[0..27]  // 28-bit transfer count (256 million transfers maximum).
    }
    // DMA Channel 15 Control and Status
    reg CH15_CTRL_TRIG offset 0x3CC {
        field AHB_ERROR: b1 bit[31] readonly  // Logical OR of the READ_ERROR and WRITE_ERROR flags. The channel halts when it encounters any bus error, and always raises its channel IRQ flag.
        field READ_ERROR: b1 bit[30]  // If 1, the channel received a read bus error. Write one to clear. 
        field WRITE_ERROR: b1 bit[29]  // If 1, the channel received a write bus error. Write one to clear. 
        field BUSY: b1 bit[26] readonly  // This flag goes high when the channel starts a new transfer sequence, and low when the last transfer of that sequence completes. Clearing EN while BUSY is high pauses the channel, and BUSY will stay high while paused.
        field SNIFF_EN: b1 bit[25]  // If 1, this channel's data transfers are visible to the sniff hardware, and each transfer will advance the state of the checksum. This only applies if the sniff hardware is enabled, and has this channel selected.
        field BSWAP: b1 bit[24]  // Apply byte-swap transformation to DMA data. 
        field IRQ_QUIET: b1 bit[23]  // In QUIET mode, the channel does not generate IRQs at the end of every transfer block. Instead, an IRQ is raised when NULL is written to a trigger register, indicating the end of a control block chain.
        field TREQ_SEL: CH0_CTRL_TRIG_TREQ_SEL bit[17..22]  // Select a Transfer Request signal. 
        field CHAIN_TO: u32 bit[13..16]  // When this channel completes, it will trigger the channel indicated by CHAIN_TO. Disable by setting CHAIN_TO = _(this channel)_.
        field RING_SEL: b1 bit[12]  // Select whether RING_SIZE applies to read or write addresses. 
        field RING_SIZE: CH0_CTRL_TRIG_RING_SIZE bit[8..11]  // Size of address wrap region. If 0, don't wrap. For values n > 0, only the lower n bits of the address will change. This wraps the address on a (1 << n) byte boundary, facilitating access to naturally-aligned ring buffers.
        field INCR_WRITE_REV: b1 bit[7]  // If 1, and INCR_WRITE is 1, the write address is decremented rather than incremented with each transfer.
        field INCR_WRITE: b1 bit[6]  // If 1, the write address increments with each transfer. If 0, each write is directed to the same, initial address.
        field INCR_READ_REV: b1 bit[5]  // If 1, and INCR_READ is 1, the read address is decremented rather than incremented with each transfer.
        field INCR_READ: b1 bit[4]  // If 1, the read address increments with each transfer. If 0, each read is directed to the same, initial address.
        field DATA_SIZE: CH0_CTRL_TRIG_DATA_SIZE bit[2..3]  // Set the size of each bus transfer (byte/halfword/word). READ_ADDR and WRITE_ADDR advance by this amount (1/2/4 bytes) with each transfer.
        field HIGH_PRIORITY: b1 bit[1]  // HIGH_PRIORITY gives a channel preferential treatment in issue scheduling: in each scheduling round, all high priority channels are considered first, and then only a single low priority channel, before returning to the high priority channels.
        field EN: b1 bit[0]  // DMA Channel Enable. 
    }
    // Alias for channel 15 CTRL register
    reg CH15_AL1_CTRL offset 0x3D0 {
        field CH15_AL1_CTRL: u32 bit[0..31]
    }
    // Alias for channel 15 READ_ADDR register
    reg CH15_AL1_READ_ADDR offset 0x3D4 {
        field CH15_AL1_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 15 WRITE_ADDR register
    reg CH15_AL1_WRITE_ADDR offset 0x3D8 {
        field CH15_AL1_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 15 TRANS_COUNT register 
    reg CH15_AL1_TRANS_COUNT_TRIG offset 0x3DC {
        field CH15_AL1_TRANS_COUNT_TRIG: u32 bit[0..31]
    }
    // Alias for channel 15 CTRL register
    reg CH15_AL2_CTRL offset 0x3E0 {
        field CH15_AL2_CTRL: u32 bit[0..31]
    }
    // Alias for channel 15 TRANS_COUNT register
    reg CH15_AL2_TRANS_COUNT offset 0x3E4 {
        field CH15_AL2_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 15 READ_ADDR register
    reg CH15_AL2_READ_ADDR offset 0x3E8 {
        field CH15_AL2_READ_ADDR: u32 bit[0..31]
    }
    // Alias for channel 15 WRITE_ADDR register 
    reg CH15_AL2_WRITE_ADDR_TRIG offset 0x3EC {
        field CH15_AL2_WRITE_ADDR_TRIG: u32 bit[0..31]
    }
    // Alias for channel 15 CTRL register
    reg CH15_AL3_CTRL offset 0x3F0 {
        field CH15_AL3_CTRL: u32 bit[0..31]
    }
    // Alias for channel 15 WRITE_ADDR register
    reg CH15_AL3_WRITE_ADDR offset 0x3F4 {
        field CH15_AL3_WRITE_ADDR: u32 bit[0..31]
    }
    // Alias for channel 15 TRANS_COUNT register
    reg CH15_AL3_TRANS_COUNT offset 0x3F8 {
        field CH15_AL3_TRANS_COUNT: u32 bit[0..31]
    }
    // Alias for channel 15 READ_ADDR register 
    reg CH15_AL3_READ_ADDR_TRIG offset 0x3FC {
        field CH15_AL3_READ_ADDR_TRIG: u32 bit[0..31]
    }
    // Interrupt Status (raw)
    reg INTR offset 0x400 {
        field INTR: u32 bit[0..15]  // Raw interrupt status for DMA Channels 0..15. Bit n corresponds to channel n. Ignores any masking or forcing. Channel interrupts can be cleared by writing a bit mask to INTR or INTS0/1/2/3.
    }
    // Interrupt Enables for IRQ 0
    reg INTE0 offset 0x404 {
        field INTE0: u32 bit[0..15]  // Set bit n to pass interrupts from channel n to DMA IRQ 0.
    }
    // Force Interrupts
    reg INTF0 offset 0x408 {
        field INTF0: u32 bit[0..15]  // Write 1s to force the corresponding bits in INTS0. The interrupt remains asserted until INTF0 is cleared.
    }
    // Interrupt Status for IRQ 0
    reg INTS0 offset 0x40C {
        field INTS0: u32 bit[0..15]  // Indicates active channel interrupt requests which are currently causing IRQ 0 to be asserted. 
    }
    // Interrupt Status (raw)
    reg INTR1 offset 0x410 {
        field INTR1: u32 bit[0..15]  // Raw interrupt status for DMA Channels 0..15. Bit n corresponds to channel n. Ignores any masking or forcing. Channel interrupts can be cleared by writing a bit mask to INTR or INTS0/1/2/3.
    }
    // Interrupt Enables for IRQ 1
    reg INTE1 offset 0x414 {
        field INTE1: u32 bit[0..15]  // Set bit n to pass interrupts from channel n to DMA IRQ 1.
    }
    // Force Interrupts
    reg INTF1 offset 0x418 {
        field INTF1: u32 bit[0..15]  // Write 1s to force the corresponding bits in INTS1. The interrupt remains asserted until INTF1 is cleared.
    }
    // Interrupt Status for IRQ 1
    reg INTS1 offset 0x41C {
        field INTS1: u32 bit[0..15]  // Indicates active channel interrupt requests which are currently causing IRQ 1 to be asserted. 
    }
    // Interrupt Status (raw)
    reg INTR2 offset 0x420 {
        field INTR2: u32 bit[0..15]  // Raw interrupt status for DMA Channels 0..15. Bit n corresponds to channel n. Ignores any masking or forcing. Channel interrupts can be cleared by writing a bit mask to INTR or INTS0/1/2/3.
    }
    // Interrupt Enables for IRQ 2
    reg INTE2 offset 0x424 {
        field INTE2: u32 bit[0..15]  // Set bit n to pass interrupts from channel n to DMA IRQ 2.
    }
    // Force Interrupts
    reg INTF2 offset 0x428 {
        field INTF2: u32 bit[0..15]  // Write 1s to force the corresponding bits in INTS2. The interrupt remains asserted until INTF2 is cleared.
    }
    // Interrupt Status for IRQ 2
    reg INTS2 offset 0x42C {
        field INTS2: u32 bit[0..15]  // Indicates active channel interrupt requests which are currently causing IRQ 2 to be asserted. 
    }
    // Interrupt Status (raw)
    reg INTR3 offset 0x430 {
        field INTR3: u32 bit[0..15]  // Raw interrupt status for DMA Channels 0..15. Bit n corresponds to channel n. Ignores any masking or forcing. Channel interrupts can be cleared by writing a bit mask to INTR or INTS0/1/2/3.
    }
    // Interrupt Enables for IRQ 3
    reg INTE3 offset 0x434 {
        field INTE3: u32 bit[0..15]  // Set bit n to pass interrupts from channel n to DMA IRQ 3.
    }
    // Force Interrupts
    reg INTF3 offset 0x438 {
        field INTF3: u32 bit[0..15]  // Write 1s to force the corresponding bits in INTS3. The interrupt remains asserted until INTF3 is cleared.
    }
    // Interrupt Status for IRQ 3
    reg INTS3 offset 0x43C {
        field INTS3: u32 bit[0..15]  // Indicates active channel interrupt requests which are currently causing IRQ 3 to be asserted. 
    }
    // Pacing (X/Y) fractional timer 
    reg TIMER0 offset 0x440 {
        field X: u32 bit[16..31]  // Pacing Timer Dividend. Specifies the X value for the (X/Y) fractional timer.
        field Y: u32 bit[0..15]  // Pacing Timer Divisor. Specifies the Y value for the (X/Y) fractional timer.
    }
    // Pacing (X/Y) fractional timer 
    reg TIMER1 offset 0x444 {
        field X: u32 bit[16..31]  // Pacing Timer Dividend. Specifies the X value for the (X/Y) fractional timer.
        field Y: u32 bit[0..15]  // Pacing Timer Divisor. Specifies the Y value for the (X/Y) fractional timer.
    }
    // Pacing (X/Y) fractional timer 
    reg TIMER2 offset 0x448 {
        field X: u32 bit[16..31]  // Pacing Timer Dividend. Specifies the X value for the (X/Y) fractional timer.
        field Y: u32 bit[0..15]  // Pacing Timer Divisor. Specifies the Y value for the (X/Y) fractional timer.
    }
    // Pacing (X/Y) fractional timer 
    reg TIMER3 offset 0x44C {
        field X: u32 bit[16..31]  // Pacing Timer Dividend. Specifies the X value for the (X/Y) fractional timer.
        field Y: u32 bit[0..15]  // Pacing Timer Divisor. Specifies the Y value for the (X/Y) fractional timer.
    }
    // Trigger one or more channels simultaneously
    reg MULTI_CHAN_TRIGGER offset 0x450 {
        field MULTI_CHAN_TRIGGER: u32 bit[0..15] writeonly  // Each bit in this register corresponds to a DMA channel. Writing a 1 to the relevant bit is the same as writing to that channel's trigger register; the channel will start if it is currently enabled and not already busy.
    }
    // Sniffer Control
    reg SNIFF_CTRL offset 0x454 {
        field OUT_INV: b1 bit[11]  // If set, the result appears inverted (bitwise complement) when read. This does not affect the way the checksum is calculated; the result is transformed on-the-fly between the result register and the bus.
        field OUT_REV: b1 bit[10]  // If set, the result appears bit-reversed when read. This does not affect the way the checksum is calculated; the result is transformed on-the-fly between the result register and the bus.
        field BSWAP: b1 bit[9]  // Locally perform a byte reverse on the sniffed data, before feeding into checksum.
        field CALC: SNIFF_CTRL_CALC bit[5..8]
        field DMACH: u32 bit[1..4]  // DMA channel for Sniffer to observe
        field EN: b1 bit[0]  // Enable sniffer
    }
    // Data accumulator for sniff hardware
    reg SNIFF_DATA offset 0x458 {
        field SNIFF_DATA: u32 bit[0..31]  // Write an initial seed value here before starting a DMA transfer on the channel indicated by SNIFF_CTRL_DMACH. The hardware will update this register each time it observes a read from the indicated channel. Once the channel completes, the final result can be read from this register.
    }
    // Debug RAF, WAF, TDF levels
    reg FIFO_LEVELS offset 0x460 {
        field RAF_LVL: u32 bit[16..23] readonly  // Current Read-Address-FIFO fill level
        field WAF_LVL: u32 bit[8..15] readonly  // Current Write-Address-FIFO fill level
        field TDF_LVL: u32 bit[0..7] readonly  // Current Transfer-Data-FIFO fill level
    }
    // Abort an in-progress transfer sequence on one or more channels
    reg CHAN_ABORT offset 0x464 {
        field CHAN_ABORT: u32 bit[0..15]  // Each bit corresponds to a channel. Writing a 1 aborts whatever transfer sequence is in progress on that channel. The bit will remain high until any in-flight transfers have been flushed through the address and data FIFOs.
    }
    // The number of channels this DMA instance is equipped with. This DMA supports up to 16 hardware channels, but can be configured with as few as one, to minimise silicon area.
    reg N_CHANNELS offset 0x468 {
        field N_CHANNELS: u32 bit[0..4] readonly
    }
    // Security configuration for channel 0. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH0 offset 0x480 {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 1. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH1 offset 0x484 {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 2. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH2 offset 0x488 {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 3. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH3 offset 0x48C {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 4. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH4 offset 0x490 {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 5. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH5 offset 0x494 {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 6. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH6 offset 0x498 {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 7. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH7 offset 0x49C {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 8. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH8 offset 0x4A0 {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 9. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH9 offset 0x4A4 {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 10. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH10 offset 0x4A8 {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 11. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH11 offset 0x4AC {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 12. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH12 offset 0x4B0 {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 13. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH13 offset 0x4B4 {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 14. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH14 offset 0x4B8 {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for channel 15. Control whether this channel performs Secure/Non-secure and Privileged/Unprivileged bus accesses.
    reg SECCFG_CH15 offset 0x4BC {
        field LOCK: b1 bit[2]  // LOCK is 0 at reset, and is set to 1 automatically upon a successful write to this channel's control registers. That is, a write to CTRL, READ_ADDR, WRITE_ADDR, TRANS_COUNT and their aliases.
        field S: b1 bit[1]  // Secure channel. If 1, this channel performs Secure bus accesses. If 0, it performs Non-secure bus accesses.
        field P: b1 bit[0]  // Privileged channel. If 1, this channel performs Privileged bus accesses. If 0, it performs Unprivileged bus accesses.
    }
    // Security configuration for IRQ 0. Control whether the IRQ permits configuration by Non-secure/Unprivileged contexts, and whether it can observe Secure/Privileged channel interrupt flags.
    reg SECCFG_IRQ0 offset 0x4C0 {
        field S: b1 bit[1]  // Secure IRQ. If 1, this IRQ's control registers can only be accessed from a Secure context.
        field P: b1 bit[0]  // Privileged IRQ. If 1, this IRQ's control registers can only be accessed from a Privileged context.
    }
    // Security configuration for IRQ 1. Control whether the IRQ permits configuration by Non-secure/Unprivileged contexts, and whether it can observe Secure/Privileged channel interrupt flags.
    reg SECCFG_IRQ1 offset 0x4C4 {
        field S: b1 bit[1]  // Secure IRQ. If 1, this IRQ's control registers can only be accessed from a Secure context.
        field P: b1 bit[0]  // Privileged IRQ. If 1, this IRQ's control registers can only be accessed from a Privileged context.
    }
    // Security configuration for IRQ 2. Control whether the IRQ permits configuration by Non-secure/Unprivileged contexts, and whether it can observe Secure/Privileged channel interrupt flags.
    reg SECCFG_IRQ2 offset 0x4C8 {
        field S: b1 bit[1]  // Secure IRQ. If 1, this IRQ's control registers can only be accessed from a Secure context.
        field P: b1 bit[0]  // Privileged IRQ. If 1, this IRQ's control registers can only be accessed from a Privileged context.
    }
    // Security configuration for IRQ 3. Control whether the IRQ permits configuration by Non-secure/Unprivileged contexts, and whether it can observe Secure/Privileged channel interrupt flags.
    reg SECCFG_IRQ3 offset 0x4CC {
        field S: b1 bit[1]  // Secure IRQ. If 1, this IRQ's control registers can only be accessed from a Secure context.
        field P: b1 bit[0]  // Privileged IRQ. If 1, this IRQ's control registers can only be accessed from a Privileged context.
    }
    // Miscellaneous security configuration
    reg SECCFG_MISC offset 0x4D0 {
        field TIMER3_S: b1 bit[9]  // If 1, the TIMER3 register is only accessible from a Secure context, and timer DREQ 3 is only visible to Secure channels.
        field TIMER3_P: b1 bit[8]  // If 1, the TIMER3 register is only accessible from a Privileged (or more Secure) context, and timer DREQ 3 is only visible to Privileged (or more Secure) channels.
        field TIMER2_S: b1 bit[7]  // If 1, the TIMER2 register is only accessible from a Secure context, and timer DREQ 2 is only visible to Secure channels.
        field TIMER2_P: b1 bit[6]  // If 1, the TIMER2 register is only accessible from a Privileged (or more Secure) context, and timer DREQ 2 is only visible to Privileged (or more Secure) channels.
        field TIMER1_S: b1 bit[5]  // If 1, the TIMER1 register is only accessible from a Secure context, and timer DREQ 1 is only visible to Secure channels.
        field TIMER1_P: b1 bit[4]  // If 1, the TIMER1 register is only accessible from a Privileged (or more Secure) context, and timer DREQ 1 is only visible to Privileged (or more Secure) channels.
        field TIMER0_S: b1 bit[3]  // If 1, the TIMER0 register is only accessible from a Secure context, and timer DREQ 0 is only visible to Secure channels.
        field TIMER0_P: b1 bit[2]  // If 1, the TIMER0 register is only accessible from a Privileged (or more Secure) context, and timer DREQ 0 is only visible to Privileged (or more Secure) channels.
        field SNIFF_S: b1 bit[1]  // If 1, the sniffer can see data transfers from Secure channels, and can itself only be accessed from a Secure context.
        field SNIFF_P: b1 bit[0]  // If 1, the sniffer can see data transfers from Privileged channels, and can itself only be accessed from a privileged context, or from a Secure context when SNIFF_S is 0.
    }
    // Control register for DMA MPU. Accessible only from a Privileged context.
    reg MPU_CTRL offset 0x500 {
        field NS_HIDE_ADDR: b1 bit[3]  // By default, when a region's S bit is clear, Non-secure-Privileged reads can see the region's base address and limit address. Set this bit to make the addresses appear as 0 to Non-secure reads, even when the region is Non-secure, to avoid leaking information about the processor SAU map.
        field S: b1 bit[2]  // Determine whether an address not covered by an active MPU region is Secure (1) or Non-secure (0)
        field P: b1 bit[1]  // Determine whether an address not covered by an active MPU region is Privileged (1) or Unprivileged (0)
    }
    // Base address register for MPU region 0. Writable only from a Secure, Privileged context.
    reg MPU_BAR0 offset 0x504 {
        field ADDR: u32 bit[5..31]  // This MPU region matches addresses where addr[31:5] (the 27 most significant bits) are greater than or equal to BAR_ADDR, and less than or equal to LAR_ADDR.
    }
    // Limit address register for MPU region 0. Writable only from a Secure, Privileged context, with the exception of the P bit.
    reg MPU_LAR0 offset 0x508 {
        field ADDR: u32 bit[5..31]  // Limit address bits 31:5. Readable from any Privileged context, if and only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is clear. Otherwise readable only from a Secure, Privileged context.
        field S: b1 bit[2]  // Determines the Secure/Non-secure (=1/0) status of addresses matching this region, if this region is enabled.
        field P: b1 bit[1]  // Determines the Privileged/Unprivileged (=1/0) status of addresses matching this region, if this region is enabled. Writable from any Privileged context, if and only if the S bit is clear. Otherwise, writable only from a Secure, Privileged context.
        field EN: b1 bit[0]  // Region enable. If 1, any address within range specified by the base address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes specified by S and P.
    }
    // Base address register for MPU region 1. Writable only from a Secure, Privileged context.
    reg MPU_BAR1 offset 0x50C {
        field ADDR: u32 bit[5..31]  // This MPU region matches addresses where addr[31:5] (the 27 most significant bits) are greater than or equal to BAR_ADDR, and less than or equal to LAR_ADDR.
    }
    // Limit address register for MPU region 1. Writable only from a Secure, Privileged context, with the exception of the P bit.
    reg MPU_LAR1 offset 0x510 {
        field ADDR: u32 bit[5..31]  // Limit address bits 31:5. Readable from any Privileged context, if and only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is clear. Otherwise readable only from a Secure, Privileged context.
        field S: b1 bit[2]  // Determines the Secure/Non-secure (=1/0) status of addresses matching this region, if this region is enabled.
        field P: b1 bit[1]  // Determines the Privileged/Unprivileged (=1/0) status of addresses matching this region, if this region is enabled. Writable from any Privileged context, if and only if the S bit is clear. Otherwise, writable only from a Secure, Privileged context.
        field EN: b1 bit[0]  // Region enable. If 1, any address within range specified by the base address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes specified by S and P.
    }
    // Base address register for MPU region 2. Writable only from a Secure, Privileged context.
    reg MPU_BAR2 offset 0x514 {
        field ADDR: u32 bit[5..31]  // This MPU region matches addresses where addr[31:5] (the 27 most significant bits) are greater than or equal to BAR_ADDR, and less than or equal to LAR_ADDR.
    }
    // Limit address register for MPU region 2. Writable only from a Secure, Privileged context, with the exception of the P bit.
    reg MPU_LAR2 offset 0x518 {
        field ADDR: u32 bit[5..31]  // Limit address bits 31:5. Readable from any Privileged context, if and only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is clear. Otherwise readable only from a Secure, Privileged context.
        field S: b1 bit[2]  // Determines the Secure/Non-secure (=1/0) status of addresses matching this region, if this region is enabled.
        field P: b1 bit[1]  // Determines the Privileged/Unprivileged (=1/0) status of addresses matching this region, if this region is enabled. Writable from any Privileged context, if and only if the S bit is clear. Otherwise, writable only from a Secure, Privileged context.
        field EN: b1 bit[0]  // Region enable. If 1, any address within range specified by the base address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes specified by S and P.
    }
    // Base address register for MPU region 3. Writable only from a Secure, Privileged context.
    reg MPU_BAR3 offset 0x51C {
        field ADDR: u32 bit[5..31]  // This MPU region matches addresses where addr[31:5] (the 27 most significant bits) are greater than or equal to BAR_ADDR, and less than or equal to LAR_ADDR.
    }
    // Limit address register for MPU region 3. Writable only from a Secure, Privileged context, with the exception of the P bit.
    reg MPU_LAR3 offset 0x520 {
        field ADDR: u32 bit[5..31]  // Limit address bits 31:5. Readable from any Privileged context, if and only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is clear. Otherwise readable only from a Secure, Privileged context.
        field S: b1 bit[2]  // Determines the Secure/Non-secure (=1/0) status of addresses matching this region, if this region is enabled.
        field P: b1 bit[1]  // Determines the Privileged/Unprivileged (=1/0) status of addresses matching this region, if this region is enabled. Writable from any Privileged context, if and only if the S bit is clear. Otherwise, writable only from a Secure, Privileged context.
        field EN: b1 bit[0]  // Region enable. If 1, any address within range specified by the base address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes specified by S and P.
    }
    // Base address register for MPU region 4. Writable only from a Secure, Privileged context.
    reg MPU_BAR4 offset 0x524 {
        field ADDR: u32 bit[5..31]  // This MPU region matches addresses where addr[31:5] (the 27 most significant bits) are greater than or equal to BAR_ADDR, and less than or equal to LAR_ADDR.
    }
    // Limit address register for MPU region 4. Writable only from a Secure, Privileged context, with the exception of the P bit.
    reg MPU_LAR4 offset 0x528 {
        field ADDR: u32 bit[5..31]  // Limit address bits 31:5. Readable from any Privileged context, if and only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is clear. Otherwise readable only from a Secure, Privileged context.
        field S: b1 bit[2]  // Determines the Secure/Non-secure (=1/0) status of addresses matching this region, if this region is enabled.
        field P: b1 bit[1]  // Determines the Privileged/Unprivileged (=1/0) status of addresses matching this region, if this region is enabled. Writable from any Privileged context, if and only if the S bit is clear. Otherwise, writable only from a Secure, Privileged context.
        field EN: b1 bit[0]  // Region enable. If 1, any address within range specified by the base address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes specified by S and P.
    }
    // Base address register for MPU region 5. Writable only from a Secure, Privileged context.
    reg MPU_BAR5 offset 0x52C {
        field ADDR: u32 bit[5..31]  // This MPU region matches addresses where addr[31:5] (the 27 most significant bits) are greater than or equal to BAR_ADDR, and less than or equal to LAR_ADDR.
    }
    // Limit address register for MPU region 5. Writable only from a Secure, Privileged context, with the exception of the P bit.
    reg MPU_LAR5 offset 0x530 {
        field ADDR: u32 bit[5..31]  // Limit address bits 31:5. Readable from any Privileged context, if and only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is clear. Otherwise readable only from a Secure, Privileged context.
        field S: b1 bit[2]  // Determines the Secure/Non-secure (=1/0) status of addresses matching this region, if this region is enabled.
        field P: b1 bit[1]  // Determines the Privileged/Unprivileged (=1/0) status of addresses matching this region, if this region is enabled. Writable from any Privileged context, if and only if the S bit is clear. Otherwise, writable only from a Secure, Privileged context.
        field EN: b1 bit[0]  // Region enable. If 1, any address within range specified by the base address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes specified by S and P.
    }
    // Base address register for MPU region 6. Writable only from a Secure, Privileged context.
    reg MPU_BAR6 offset 0x534 {
        field ADDR: u32 bit[5..31]  // This MPU region matches addresses where addr[31:5] (the 27 most significant bits) are greater than or equal to BAR_ADDR, and less than or equal to LAR_ADDR.
    }
    // Limit address register for MPU region 6. Writable only from a Secure, Privileged context, with the exception of the P bit.
    reg MPU_LAR6 offset 0x538 {
        field ADDR: u32 bit[5..31]  // Limit address bits 31:5. Readable from any Privileged context, if and only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is clear. Otherwise readable only from a Secure, Privileged context.
        field S: b1 bit[2]  // Determines the Secure/Non-secure (=1/0) status of addresses matching this region, if this region is enabled.
        field P: b1 bit[1]  // Determines the Privileged/Unprivileged (=1/0) status of addresses matching this region, if this region is enabled. Writable from any Privileged context, if and only if the S bit is clear. Otherwise, writable only from a Secure, Privileged context.
        field EN: b1 bit[0]  // Region enable. If 1, any address within range specified by the base address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes specified by S and P.
    }
    // Base address register for MPU region 7. Writable only from a Secure, Privileged context.
    reg MPU_BAR7 offset 0x53C {
        field ADDR: u32 bit[5..31]  // This MPU region matches addresses where addr[31:5] (the 27 most significant bits) are greater than or equal to BAR_ADDR, and less than or equal to LAR_ADDR.
    }
    // Limit address register for MPU region 7. Writable only from a Secure, Privileged context, with the exception of the P bit.
    reg MPU_LAR7 offset 0x540 {
        field ADDR: u32 bit[5..31]  // Limit address bits 31:5. Readable from any Privileged context, if and only if this region's S bit is clear, and MPU_CTRL_NS_HIDE_ADDR is clear. Otherwise readable only from a Secure, Privileged context.
        field S: b1 bit[2]  // Determines the Secure/Non-secure (=1/0) status of addresses matching this region, if this region is enabled.
        field P: b1 bit[1]  // Determines the Privileged/Unprivileged (=1/0) status of addresses matching this region, if this region is enabled. Writable from any Privileged context, if and only if the S bit is clear. Otherwise, writable only from a Secure, Privileged context.
        field EN: b1 bit[0]  // Region enable. If 1, any address within range specified by the base address (BAR_ADDR) and limit address (LAR_ADDR) has the attributes specified by S and P.
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH0_DBG_CTDREQ offset 0x800 {
        field CH0_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH0_DBG_TCR offset 0x804 {
        field CH0_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH1_DBG_CTDREQ offset 0x840 {
        field CH1_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH1_DBG_TCR offset 0x844 {
        field CH1_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH2_DBG_CTDREQ offset 0x880 {
        field CH2_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH2_DBG_TCR offset 0x884 {
        field CH2_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH3_DBG_CTDREQ offset 0x8C0 {
        field CH3_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH3_DBG_TCR offset 0x8C4 {
        field CH3_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH4_DBG_CTDREQ offset 0x900 {
        field CH4_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH4_DBG_TCR offset 0x904 {
        field CH4_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH5_DBG_CTDREQ offset 0x940 {
        field CH5_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH5_DBG_TCR offset 0x944 {
        field CH5_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH6_DBG_CTDREQ offset 0x980 {
        field CH6_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH6_DBG_TCR offset 0x984 {
        field CH6_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH7_DBG_CTDREQ offset 0x9C0 {
        field CH7_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH7_DBG_TCR offset 0x9C4 {
        field CH7_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH8_DBG_CTDREQ offset 0xA00 {
        field CH8_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH8_DBG_TCR offset 0xA04 {
        field CH8_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH9_DBG_CTDREQ offset 0xA40 {
        field CH9_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH9_DBG_TCR offset 0xA44 {
        field CH9_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH10_DBG_CTDREQ offset 0xA80 {
        field CH10_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH10_DBG_TCR offset 0xA84 {
        field CH10_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH11_DBG_CTDREQ offset 0xAC0 {
        field CH11_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH11_DBG_TCR offset 0xAC4 {
        field CH11_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH12_DBG_CTDREQ offset 0xB00 {
        field CH12_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH12_DBG_TCR offset 0xB04 {
        field CH12_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH13_DBG_CTDREQ offset 0xB40 {
        field CH13_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH13_DBG_TCR offset 0xB44 {
        field CH13_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH14_DBG_CTDREQ offset 0xB80 {
        field CH14_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH14_DBG_TCR offset 0xB84 {
        field CH14_DBG_TCR: u32 bit[0..31] readonly
    }
    // Read: get channel DREQ counter (i.e. how many accesses the DMA expects it can perform on the peripheral without overflow/underflow. Write any value: clears the counter, and cause channel to re-initiate DREQ handshake.
    reg CH15_DBG_CTDREQ offset 0xBC0 {
        field CH15_DBG_CTDREQ: u32 bit[0..5]
    }
    // Read to get channel TRANS_COUNT reload value, i.e. the length of the next transfer
    reg CH15_DBG_TCR offset 0xBC4 {
        field CH15_DBG_TCR: u32 bit[0..31] readonly
    }
}

