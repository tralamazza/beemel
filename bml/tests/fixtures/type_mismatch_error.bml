// Error: arithmetic/comparison between different types (E310, E311)
fn main() @context(thread) {
    var a: u32 = 10;
    var b: i32 = -5;
    var c: f32 = 1.5f;

    var bad_arith = a + b;   // E310: arithmetic between different types
    if a == b {              // E311: comparison between different types
        var x: u32 = 1;
    }
    var bad_mixed = a + c;   // E310: arithmetic between different types
}
