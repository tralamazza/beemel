// ISR(priority=2) accessing @shared(ceiling=2) -- 2 > 2 is false → NO critical section

var counter: u32 @shared(ceiling = 2);

fn same_isr() @isr(priority = 2) {
    var x: u32 = counter;
    counter = x + 1;
}
