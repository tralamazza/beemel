// A peripheral_type template in its own module (think lib/ip/gpio.bml), to be
// instantiated from another file -- see peripheral_type_crossfile_ok.bml.
peripheral_type GpioPort {
    reg MODER offset 0x00 { field MODE0: u32 bit[0..1] }
    reg ODR offset 0x14 { field ODR0: b1 bit[0] }
}
