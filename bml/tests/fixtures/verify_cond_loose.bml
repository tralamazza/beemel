// Conditional narrowing isn't tight enough: `if i < 10` still permits
// indices 4..=9 which are out of bounds. IKOS must report V101.
fn lookup(i: u32) @context(thread) {
    var buf: [u32; 4] = [1u32, 2u32, 3u32, 4u32];
    if i < 10u32 {
        var x: u32 = buf[i];
    }
}
