// Mutable bit view: set a bit by index (read-modify-write of one byte).
fn main() @context(thread) {
    var buf: [u8; 4] = [0u8, 0u8, 0u8, 0u8];
    var b: bits mut = bits(buf);
    b[5u32] = true;
}
