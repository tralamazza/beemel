// Thread context accessing @shared(ceiling=2) -- emits BASEPRI_MAX to the
// ceiling (0x20) with save/restore; the global cpsid mask must NOT appear.

var counter: u32 @shared(ceiling = 2);

fn main() @context(thread) {
    var x: u32 = counter;
    counter = x + 1;
}
