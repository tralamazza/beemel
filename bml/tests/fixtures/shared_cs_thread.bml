// Thread context accessing @shared(ceiling=2) -- must emit cpsid i / cpsie i

static counter: u32 @shared(ceiling = 2);

fn main() @context(thread) {
    var x: u32 = counter;
    counter = x + 1;
}
