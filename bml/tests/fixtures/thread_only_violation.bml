// Error: ISR accessing unannotated static (thread-only by default)
static counter: u32;

fn main() @context(thread) {
    var x: u32 = counter;  // OK -- thread context
}

fn timer_isr() @isr(priority = 1) {
    var y: u32 = counter;  // ERROR -- unannotated static is thread-only
}
