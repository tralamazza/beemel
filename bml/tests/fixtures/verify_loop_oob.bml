// For loop that walks past the array. IKOS must report a V101
// buffer-overflow warning on the indexing inside the loop body.
fn main() @context(thread) {
    var buf: [u32; 4] = [1u32, 2u32, 3u32, 4u32];
    for i: u32 in 0 upto 10 {
        var x: u32 = buf[i];
    }
}
