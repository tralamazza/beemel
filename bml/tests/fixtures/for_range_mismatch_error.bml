// Error: for loop bound type does not match declared loop variable type.
// `0u8` is a suffixed literal of type u8, so it does not fit the u32 declaration.
fn main() @context(thread) {
    for i: u32 in 0u8 upto 10 {  // E312: start bound type mismatch
        var x: u32 = i;
    }
}
