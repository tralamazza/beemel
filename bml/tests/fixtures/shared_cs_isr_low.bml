// ISR(priority=3) accessing @shared(ceiling=2) -- 3 > 2 → needs a critical
// section (BASEPRI_MAX to the ceiling on this v7-M target).

var counter: u32 @shared(ceiling = 2);

fn low_isr() @isr(priority = 3) {
    var x: u32 = counter;
    counter = x + 1;
}
