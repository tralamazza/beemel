enum State: u32 {
    Idle,
    Running,
    Error,
}

fn main() @context(thread) {
    var s = State@Idle;
    match s {
        State@Idle {
            var x = 0;
        }
        _ {
            var x = 1;
        }
    }
}
