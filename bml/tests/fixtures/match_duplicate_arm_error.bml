enum State: u32 {
    Idle,
    Running,
}

fn main() @context(thread) {
    var s = State@Idle;
    match s {
        State@Idle {
            var x = 0;
        }
        State@Idle {
            var x = 1;
        }
    }
}
