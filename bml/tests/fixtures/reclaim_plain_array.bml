// `reclaim(x)` applies only to agent-shared memory; reclaiming a plain array is
// rejected (E335) -- a plain array is never the agent's, so use `view(x)`.
var BUF: [u32; 4] = [0u32, 0u32, 0u32, 0u32];

fn main() @context(thread) {
    const v: view u32 = reclaim(BUF);
    var a: u32 = v[0u32];
}
