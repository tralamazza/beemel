// The index-borrow fix is shared across view kinds: two index reads of a
// `bits mut` are both valid (no consume).
fn main() @context(thread) {
    var buf: [u8; 4] = [0u8, 0u8, 0u8, 0u8];
    var b: bits mut = bits(buf);
    var x: b1 = b[0u32];
    var y: b1 = b[1u32];
}
